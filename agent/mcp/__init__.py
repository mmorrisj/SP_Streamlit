"""MCP client integration for the in-process orchestrator.

This package lets the orchestrator (which runs synchronously inside FastAPI)
invoke tools that live in separate processes over the real MCP wire protocol.
"""
