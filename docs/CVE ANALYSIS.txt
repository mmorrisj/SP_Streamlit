mmorrisj/pgvector 0.8.1 pg16-bookworm   3March2026

CVE-2023-45853	mmorrisj/pgvector	2-Mar-26	False positive	Not applicable — CVE-2023-45853 (CVSS 9.8 nominal, but effectively a false positive for this image) describes an integer overflow and heap-based buffer overflow in MiniZip's `zipOpenNewFileInZip4_64` function via a long filename, comment, or extra field. Despite the CRITICAL NVD score, this CVE targets the `contrib/minizip` code path within the zlib source tree, **not** the `zlib1g` binary package installed in the image. Debian split MiniZip into a separate `libminizip1` source/binary package over a decade ago; the `zlib1g` package in Bookworm does not contain the vulnerable `zipOpenNewFileInZip4_64` function, and neither `libminizip1` nor `pyminizip` are installed in the pgvector image. The Debian security tracker explicitly marks this as `<ignored>` for `src:zlib` in Bookworm with the note "contrib/minizip not built and src:zlib not producing binary packages." Enterprise scanners (Trivy, Grype, Docker Scout) commonly report this as a false positive because they match the CVE against the `zlib` source package name without checking whether the vulnerable MiniZip code is actually present in the binary package. This is already documented as an exception in the existing CVE mitigation report.
CVE-2023-2953	mmorrisj/pgvector	2-Mar-26	Not Applicable	Not applicable — postgres:16-bookworm ships libldap-2.5-0 2.5.13, which contains the vulnerable ber_memalloc_x() NULL pointer dereference (fix is in upstream 2.5.14+; Debian classifies it <no-dsa>, minor issue); however, the exploit path is unreachable because libldap is present only as a transitive dependency of libpq for optional LDAP authentication support, and this deployment uses password authentication over the Docker network with no LDAP URIs, ldap.conf, SASL-LDAP bindings, or slapd server installed — the vulnerable code path is only entered during active LDAP operations, which are never performed.
CVE-2026-0861	mmorrisj/pgvector	2-Mar-26	Not applicable	 CVE-2026-0861 affects the glibc memalign family of functions (memalign, posix_memalign, aligned_alloc), where an excessively large alignment value triggers an integer overflow that can corrupt the heap. The vulnerability requires a local process to call these functions with attacker-controlled alignment parameters. PostgreSQL does not use glibc's aligned allocation APIs — it manages memory through its own palloc/MemoryContext subsystem, and no application code in the container invokes memalign with user-supplied values. The container runs as USER postgres with no interactive shell access; the only entry point is the PostgreSQL wire protocol on port 5432. Debian classifies this as <no-dsa> (minor issue), and no fixed glibc package is available for Trixie. The affected packages (libc-bin, libc-l10n, libc6, locales) are unremovable core dependencies of every binary in the image and their vulnerable code paths are never reached at runtime.
CVE-2023-50495	mmorrisj/pgvector	2-Mar-26	Not applicable	 CVE-2023-50495 is a segmentation fault in ncurses' _nc_wrap_entry() triggered during parsing of terminfo database files (CVSS 6.5 MEDIUM, Debian <no-dsa>). Bookworm ships ncurses 6.4-4 (vulnerable; fix is in upstream 6.4-20230424 patchlevel). The vulnerable code path requires parsing a crafted or malicious terminfo file; terminfo files are trusted system data, and since CVE-2023-29491 ncurses no longer parses them for setuid applications. The pgvector container runs PostgreSQL — it does not process user-supplied terminfo files, has no interactive terminal sessions, and ncurses is present only as an OS base dependency (libtinfo6) for readline/CLI tooling; the exploit path is unreachable in this deployment.
CVE-2024-10041	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-10041 is a speculative-execution side-channel in PAM's pam_unix module that could leak /etc/shadow passwords during authentication (moderate severity). The fix switches shadow-file reads to the external unix-chkpwd helper. Debian marks this <ignored> for bookworm, noting the fix requires src:apparmor changes and may break SELinux. The pgvector container does not perform PAM-based authentication — PostgreSQL uses its own pg_hba.conf password authentication over the Docker network, /etc/shadow is not consulted during any database operation, and no interactive login or su/sudo/sshd services run in the container. The libpam libraries are present as base OS dependencies but the vulnerable pam_unix authentication code path is never invoked.
CVE-2025-10911	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-10911 is a use-after-free in libxslt triggered while parsing XSL nodes with cross-RVT key references, primarily a DoS vector via crash when processing untrusted XSL/XML transformations. libxslt is present in the bookworm base image as a transitive OS dependency but is not invoked by PostgreSQL or the pgvector extension — the database engine processes SQL queries over its wire protocol, not XML/XSL transformations. No XSLT processing, xsltproc invocation, or application code path in this container accepts or transforms untrusted XML/XSL input, so the vulnerable code path is unreachable.
CVE-2025-13151	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-13151 is a stack-based buffer overflow in libtasn1's asn1_expand_octet_string function (v4.20.0; fix in 4.21.0) that can crash applications processing untrusted ASN.1 data. Bookworm ships libtasn1-6 version 4.19.0, which predates the vulnerable 4.20.0 release — however, even if the bookworm version were affected, libtasn1 is a transitive dependency pulled in by the GnuTLS/Kerberos library chain and is only exercised during TLS certificate parsing or Kerberos ticket processing. The pgvector container uses PostgreSQL's own OpenSSL-based TLS stack (not GnuTLS) for any connection encryption, does not perform Kerberos authentication, and does not process untrusted ASN.1 input at any point — the vulnerable asn1_expand_octet_string code path is unreachable in this deployment.
CVE-2025-14104	mmorrisj/pgvector	2-Mar-26	Not Applicable	 CVE-2025-14104 is a heap buffer overread in setpwnam() within util-linux's SUID login-utils triggered by processing 256-byte usernames when writing to the password database. The pgvector container runs as USER postgres (non-root) with no privileged capabilities, does not perform local user account management, and does not invoke SUID login utilities (newgrp, chfn, chsh, login, etc.) — PostgreSQL manages its own authentication via pg_hba.conf over the network, not through system password files. The vulnerable setpwnam() code path requires a SUID binary to be executed with a crafted 256-byte username, which is unreachable in a container running only the PostgreSQL daemon.
CVE-2025-15281	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-15281 is a glibc vulnerability where wordexp() called with WRDE_REUSE | WRDE_APPEND flags returns uninitialized memory, causing a process abort (DoS) when wordfree() is called or we_wordv is inspected (CVSS 7.5, affects glibc up to 2.42; bookworm ships glibc 2.36). PostgreSQL does not call wordexp() — it is a shell word-expansion function used for glob/tilde/variable expansion in shell-like contexts. The PostgreSQL daemon processes SQL queries and has no code path that invokes wordexp() with any flag combination, nor does the pgvector extension. The vulnerable function is unreachable in this deployment.
CVE-2025-30258	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-30258 is a DoS in GnuPG's signature verification triggered when a user imports a certificate with crafted subkey data lacking a valid backsig (CVSS 4.7 MEDIUM, local access required). GnuPG (gpg) and related packages (gnupg, gpg-wks-client, gpg-wks-server) were explicitly purged from the pgvector image during build (see Section 5.1 "Runtime package reduction"). Even if residual libgpg-error/libgcrypt libraries remain as transitive dependencies, the vulnerable code path is in the gpg binary's g10/getkey.c key-fetching logic — with gpg removed, no certificate import or signature verification can occur in this container. The exploit path is absent.
CVE-2025-68972	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-68972 is a signature verification bypass in GnuPG through 2.4.8 where a \f character at the end of a plaintext line allows an adversary to append unsigned text to a signed message while verification still succeeds (CVSS 5.9 MEDIUM). GnuPG (gnupg, gpg, gpg-wks-client, gpg-wks-server) was explicitly purged from the pgvector image during build. With the gpg binary removed, no signature verification operations can occur in this container — the vulnerable code path is absent entirely.
CVE-2026-0915	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-0915 is a glibc information leak where getnetbyaddr() / getnetbyaddr_r() with a zero-valued network input sends uninitialized stack contents to a DNS resolver when nsswitch.conf configures the DNS backend for networks lookups (CVSS 0.0, glibc 2.0–2.42). This is closely related to the already-documented CVE-2026-0861 (Section 5.1.1) — the same mitigation applies: the pgvector container's /etc/nsswitch.conf has networks: files, not networks: dns, so the glibc DNS backend for network lookups is never invoked. Additionally, PostgreSQL does not call getnetbyaddr() during normal operation, and no application code path triggers network-address-to-name resolution. The exploit path is unreachable in this deployment.
CVE-2026-0990	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-0990 is an uncontrolled recursion vulnerability in libxml2's xmlCatalogXMLResolveURI function triggered when an XML catalog contains a self-referencing delegate URI entry, causing a stack overflow / DoS. While libxml2 is present in the bookworm base image as a transitive OS dependency, PostgreSQL does not use libxml2's XML catalog resolution functionality — PostgreSQL's built-in XML support (xml data type, xpath(), etc.) uses libxml2 for XML parsing but does not process XML catalogs. The vulnerable xmlCatalogXMLResolveURI code path requires loading a crafted XML catalog file, which is not performed by PostgreSQL during normal operation, and no user-supplied XML catalogs are processed by this container.
CVE-2026-27171	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-27171 is a CPU exhaustion vulnerability in zlib's crc32_combine64 / crc32_combine_gen64 functions where x2nmodp can enter an infinite loop due to a missing termination condition (CVSS 2.9 LOW, local access, high complexity). Bookworm ships zlib 1:1.2.13 (vulnerable; fix is in 1.3.2). The vulnerable crc32_combine64 function is used to combine CRC-32 checksums from independently compressed segments — it is not invoked during normal compress/decompress/crc32 operations. PostgreSQL uses zlib for pg_dump compression and WAL compression but does not call crc32_combine64; it computes CRC-32 values sequentially, not by combining independent segments. The attack requires an attacker to supply crafted input that triggers crc32_combine64 with a pathological length argument, which no PostgreSQL code path provides. The exploit path is unreachable in this deployment.
CVE-2005-2541	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2005-2541 is a 20-year-old finding in GNU tar where it does not warn users when extracting setuid/setgid files, potentially allowing a malicious archive to place privileged binaries on disk. The tar binary's rmt (remote tape server) component was already explicitly removed from the pgvector image (RUN rm -f /usr/sbin/rmt). Even with tar itself still present as a base OS utility, the container runs as USER postgres (non-root) — extracted files cannot gain setuid/setgid privileges because only root can create setuid files. Additionally, no user-supplied tar archives are processed by this container; tar is not invoked during PostgreSQL operation. Debian has carried this CVE as a <no-dsa> documentation-level issue for over two decades with no plan to fix it.
CVE-2007-5686	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2007 finding concerns insecure permissions on /var/log/btmp set by initscripts in rPath Linux, potentially exposing login failure data to local users. The pgvector container runs as USER postgres (non-root) with no interactive logins, no SSH, no PAM-based authentication, and no login accounting services active; PostgreSQL manages authentication entirely through pg_hba.conf, and the shadow utilities and login tracking files are never consulted during database operation.
CVE-2010-4756	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2010 finding concerns glibc's glob() function where GLOB_TILDE home-directory expansion does not limit path length, theoretically allowing local resource consumption if an attacker controls both the glob pattern and the user database. No API or pipeline script in this project accepts untrusted glob patterns from external input, Python's glob.glob() does not enable tilde expansion (that requires explicit os.path.expanduser() which is not called on user input), all containers run as non-root with /sbin/nologin, and the glibc maintainers have declined to patch this across all versions, classifying it as a theoretical local DoS — it is universally flagged by container scanners on every glibc-based image.
CVE-2011-3374	mmorrisj/pgvector	2-Mar-26	Not Applicable	This 2011 finding concerns APT's apt-key signature verification bypass during Release file validation. Both Dockerfiles run apt-get install only at build time from official Debian/PostgreSQL HTTPS repositories, then purge build tools and delete package lists; Debian 12 uses /etc/apt/keyrings/ with signed-by rather than the vulnerable apt-key codepath; the gnupg/gpg packages are explicitly purged in the pgvector image; and the final containers have no functional package manager at runtime, so the vulnerability cannot be exercised even with shell access (which is itself mitigated by non-root users with no login shell).
CVE-2011-3389	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this is the 2011 BEAST attack exploiting predictable IVs in TLS 1.0 CBC cipher suites via man-in-the-middle with chosen-plaintext injection in a browser context. Debian 12's OpenSSL 3.0 disables TLS 1.0/1.1 by default, Python 3.13's ssl module enforces a minimum of TLS 1.2, PostgreSQL 16 defaults to ssl_min_protocol_version = TLSv1.2, the server-side containers (FastAPI, Streamlit, PostgreSQL) are not browsers and lack the same-origin JavaScript injection vector the attack requires, and inter-service traffic flows over Docker's isolated bridge network rather than untrusted infrastructure.
CVE-2011-4116	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2011 finding concerns Perl's File::Temp module creating temporary files insecurely via _gettemp(), potentially allowing symlink attacks in world-writable directories like /tmp. Neither Dockerfile installs Perl as an explicit dependency; any minimal Perl present in the base Debian image is a transitive dependency of system packages and is never invoked at runtime by the application — the FastAPI server, Streamlit dashboard, and PostgreSQL database use Python and C respectively, no pipeline script shells out to Perl, File::Temp is never called, and the non-root container users (postgres, appuser with /sbin/nologin) operating in isolated Docker filesystems with no world-writable temp directories shared across trust boundaries eliminate the symlink race condition this CVE requires.
CVE-2013-4392	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2013 finding concerns systemd's journald following symlinks in /var/log/journal during persistent journal creation, potentially allowing local attackers to escalate privileges or corrupt files via symlink attacks. The Docker containers in this project do not run systemd as an init system — the pgvector container uses the PostgreSQL entrypoint directly and the application container uses supervisord as its process manager; journald is neither running nor configured in any container; container logging is handled by Docker's logging driver (stdout/stderr captured by docker-compose logs), not by systemd journal files; /var/log/journal does not exist in the container filesystems; and the non-root runtime users (USER postgres, USER appuser) could not create or manipulate journal directories even if systemd were present.
CVE-2015-3276	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2015 finding concerns OpenLDAP's nss_parse_ciphers() function incorrectly parsing NSS-style TLS cipher suite strings, potentially causing unexpected cipher selection when OpenLDAP is compiled against Mozilla NSS. The application containers in this project do not use LDAP for any purpose — authentication is handled by PostgreSQL's pg_hba.conf and application-level session management, no LDAP client libraries are installed as explicit dependencies, the registry.Dockerfile explicitly removes postgresql-client and its transitive libldap2 dependency chain (noted at line 47-48), the pgvector container purges all non-essential packages after build, and Debian Bookworm's OpenLDAP is compiled against OpenSSL (not Mozilla NSS), meaning the vulnerable NSS cipher-parsing codepath does not exist in any library that may remain as a transitive base-image dependency.
CVE-2015-9019	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2015 finding concerns libxslt not properly initializing random number generation, causing insufficient randomness in its cryptographic key derivation when generating XSLT generate-id() output or other internal random values. No component of this project uses XSLT transformations — the data pipeline ingests JSON and raw text documents from S3, AI analysis uses OpenAI API calls, and the frontend is React/TypeScript with a Streamlit dashboard; libxslt is not an explicit dependency in any Dockerfile or requirements.txt; no Python library in the stack (FastAPI, Streamlit, SQLAlchemy, sentence-transformers, langchain) depends on libxslt at runtime; and even if a trace of the library exists in the base Debian image as a transitive dependency, it is never loaded or invoked, so the weak randomness condition cannot be reached.
CVE-2016-2781	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2016 finding concerns GNU coreutils' chroot command not properly restricting the ability to call TIOCSTI ioctl to inject characters into the terminal of a calling process, potentially allowing a chroot-confined attacker to escape input isolation. The Docker containers in this project never use the chroot command — process isolation is provided by Docker's namespace and cgroup mechanisms, not by chroot jails; no entrypoint, script, or runtime operation invokes chroot; the containers run as non-root users (USER postgres, USER appuser with /sbin/nologin) who lack the privileges required to call chroot in the first place; the TIOCSTI ioctl has been disabled by default in Linux kernels 6.2+ and is further blocked by Docker's default seccomp profile; and the vulnerability requires an interactive terminal session within a chroot environment, which does not exist in these headless service containers.
CVE-2017-14159	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2017 finding concerns OpenLDAP's slapd daemon where ldap_pvt_thread_pool_getkey allows out-of-bounds writes when a caller provides a NULL key argument, potentially causing the LDAP server process to crash. This project does not run an LDAP server — slapd is not installed, configured, or executed in any container; authentication is handled entirely by PostgreSQL's pg_hba.conf and application-level logic; the registry.Dockerfile explicitly removes the postgresql-client package and its transitive libldap2 dependency chain to reduce attack surface; the pgvector container purges all non-essential packages after building pgvector; and even where minimal OpenLDAP client libraries may remain as deep transitive dependencies in the base Debian image, the vulnerable component is the slapd server daemon, not the client library — no LDAP server process exists in these containers to receive the malformed request this CVE requires.
CVE-2017-17740	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2017 finding concerns OpenLDAP's contrib/slapd-modules/nops/nops.c module where a NULL pointer dereference in the slapd server allows a remote attacker to crash the LDAP daemon by sending a crafted search request with a malformed BER element. This project does not run an LDAP server in any capacity — slapd is not installed and no LDAP service is configured or listening in any container; the vulnerable code is specifically in an optional contributed module (nops) that must be explicitly compiled and loaded into slapd, which is not present; the registry.Dockerfile explicitly removes postgresql-client and its transitive LDAP library chain; and the vulnerability is exclusively a server-side issue in the slapd daemon, not in client libraries, so even trace libldap remnants in the base Debian image are entirely unaffected since no LDAP server process exists to receive the malicious search request.
CVE-2017-18018	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2017 finding concerns GNU coreutils' chown and chgrp commands being vulnerable to a TOCTOU (time-of-check-time-of-use) race condition when the --recursive flag is used without --no-dereference, potentially allowing a local attacker to replace a directory with a symlink between the ownership check and the actual change, thereby modifying ownership of arbitrary files. The Dockerfiles in this project do not use chown --recursive on any user-writable or shared directory at runtime — the chown -R call in registry.Dockerfile (line 136) runs at image build time within a controlled, single-process build environment where no concurrent attacker process exists to exploit the race; at runtime the containers execute as non-root users (USER postgres, USER appuser) who lack the privilege to call chown at all; no entrypoint script or application code invokes chown or chgrp; and Docker's isolated filesystem namespaces prevent external processes from manipulating paths within the container to set up the symlink race this CVE requires.
CVE-2018-20796	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2018 finding concerns a stack-based buffer overflow in glibc's check_dst_limits_calc_pos_1 function within posix/regexec.c, potentially causing a denial of service (crash or excessive resource consumption) when processing crafted regular expressions. The vulnerability requires an attacker to supply a maliciously crafted regex pattern that is compiled and executed by the C-level POSIX regexec() function; Python's re module (used throughout this project) does not use glibc's POSIX regex engine — it uses its own independent regex implementation built into CPython (Modules/_sre/sre.c); no application code, pipeline script, or service in this project passes untrusted user input to C-level POSIX regex functions; PostgreSQL's regex engine is also independent of glibc's regexec(); the glibc maintainers have classified this as a low-severity issue and have not issued a formal patch; and it is universally flagged by container scanners on every glibc-based image regardless of whether the vulnerable codepath is reachable.
CVE-2018-5709	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2018 finding concerns MIT Kerberos 5 (krb5) where the kadmin/dbutil utility's process_db_args() function in plugins/kdb/ldap/ldap_util/kdb5_ldap_util.c performs an integer cast incorrectly when handling the lockdown_keys database policy attribute, potentially causing a truncation that could lead to a denial of service or unintended policy behavior. This project does not use Kerberos authentication in any capacity — PostgreSQL connections use password-based authentication via pg_hba.conf and environment variables, not GSSAPI/Kerberos; no krb5 KDC or kadmin server runs in any container; the kadmin/dbutil tool (the specifically vulnerable component) is not installed or invoked; the registry.Dockerfile explicitly removes postgresql-client and its transitive dependency chain which includes Kerberos libraries; and even where minimal libkrb5 libraries may exist as deep transitive dependencies in the base Debian image for potential GSSAPI support, the vulnerable codepath is in the Kerberos administration server tooling interacting with an LDAP backend — a configuration entirely absent from this project.
CVE-2018-6829	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2018 finding concerns Libgcrypt's ElGamal encryption implementation lacking proper protection against side-channel attacks, potentially allowing an attacker to recover plaintext by observing timing or power analysis variations during decryption of ciphertexts crafted with non-standard padding. This project does not use ElGamal encryption or Libgcrypt directly — all cryptographic operations are handled by OpenSSL (TLS connections to PostgreSQL, S3, and OpenAI API endpoints) and Python's built-in hashlib/ssl modules; Libgcrypt may be present in the base Debian image as a transitive dependency of GnuPG, but gnupg/gpg packages are explicitly purged in the pgvector Dockerfile (line 49) and are never installed in the registry Dockerfile; no application code performs ElGamal or any public-key encryption locally; the side-channel attack requires either physical proximity (power analysis) or co-located process access to the decryption operation, neither of which applies to isolated Docker containers performing no Libgcrypt cryptographic operations; and even in the theoretical case where the library is loaded, without any ElGamal key material or decryption calls the vulnerable codepath is entirely unreachable.
CVE-2019-1010022	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2019 finding concerns glibc's stack guard (stack_chk_fail) stack protection mechanism allegedly being bypassable, which could theoretically weaken stack smashing protections. This CVE is disputed by the glibc maintainers and the broader security community — it was filed by a third party and rejected as invalid because the described "bypass" requires an attacker to already have arbitrary memory write capability, at which point stack canary protection is inherently moot regardless of implementation; the National Vulnerability Database lists it as disputed; Debian's security tracker classifies it as "not a vulnerability"; the project's application code is written in Python and TypeScript (memory-safe languages) where stack buffer overflows do not occur; PostgreSQL is compiled with standard stack protections and has no known exploitable stack overflow that would make canary bypass relevant; the non-root container users and Docker's seccomp/namespace isolation provide independent layers of defense; and this CVE is universally flagged by container scanners on every glibc-based image as a known false positive that cannot be "fixed" because there is no actual bug to patch.
CVE-2019-1010023	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2019 finding claims glibc's ld.so dynamic linker leaks ASLR (Address Space Layout Randomization) base addresses through the __libc_dlopen_mode function and link_map data structures, theoretically weakening exploit mitigations. This CVE is disputed — it was filed by the same third party as CVE-2019-1010022 and is rejected by glibc maintainers and Debian's security tracker as "not a vulnerability" because the described information leak requires an attacker to already have the ability to read arbitrary process memory, at which point ASLR is inherently defeated regardless of ld.so behavior; the project's application code is written in Python and TypeScript (memory-safe languages) where the memory-disclosure primitives needed to exploit this do not arise; PostgreSQL's process isolation and Docker's namespace/seccomp confinement provide independent barriers preventing cross-process memory reads; no untrusted native code is loaded or executed within the containers; and this CVE is universally flagged by container scanners on every glibc-based image as a known false positive with no actionable remediation since no actual bug exists to patch.
CVE-2019-1010024	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2019 finding claims glibc's ASLR implementation is weakened because /proc/[pid]/maps and similar procfs interfaces expose memory layout information, theoretically allowing a local attacker to bypass ASLR. This CVE is disputed by the same grounds as CVE-2019-1010022 and CVE-2019-1010023 — it was filed by the same third party, glibc maintainers reject it as "not a vulnerability" because /proc/pid/maps is a kernel feature not a glibc bug and is access-controlled by ptrace_scope and PID namespace isolation; Debian's security tracker classifies it as invalid; Docker's PID namespace isolation prevents container processes from reading the /proc/[pid]/maps of processes outside their namespace; the default Docker seccomp profile and ptrace_scope=1 (Debian Bookworm default) further restrict access; the non-root container users (postgres, appuser) cannot read other users' process maps even within the container; and this is a universal scanner false positive on every glibc-based image with no remediation path because the "fix" would require kernel-level changes to procfs, not a glibc patch.
CVE-2019-1010025	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2019 finding claims glibc's mmap usage produces predictable address offsets that weaken ASLR entropy, theoretically making brute-force attacks against memory layouts more feasible. This is the fourth in the series of disputed CVEs (CVE-2019-1010022 through 1010025) filed by the same third party, all rejected by glibc maintainers and Debian's security tracker as "not a vulnerability" because mmap address selection is a kernel responsibility, not a glibc bug — the kernel's mmap_rnd_bits controls randomization entropy and is unrelated to glibc's code; exploiting reduced ASLR entropy still requires an independent memory corruption or disclosure primitive which does not exist in this project's memory-safe Python/TypeScript application code; Docker's namespace isolation, seccomp profiles, and non-root users provide layered defenses independent of ASLR; and as with the rest of this disputed series, it is a universal scanner false positive on every glibc-based image with no glibc-side remediation possible since the alleged weakness is in kernel virtual memory management, not in the C library.
CVE-2019-9192	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2019 finding concerns glibc's check_dst_limits_calc_pos_1 function in posix/regexec.c causing uncontrolled recursion and stack exhaustion when processing certain crafted regular expressions, leading to a denial of service. This CVE is disputed — it is closely related to (and by many assessments a duplicate of) CVE-2018-20796, and glibc maintainers have rejected it as "not a vulnerability" because it describes expected behavior when an application passes pathological regex patterns to the POSIX regex engine without imposing its own resource limits; Python's re module (used throughout this project) does not use glibc's POSIX regexec() — it uses CPython's own independent regex engine (Modules/_sre/sre.c); PostgreSQL similarly uses its own regex implementation, not glibc's; no application code or pipeline script passes untrusted user input to C-level POSIX regex functions; the non-root container users and Docker's resource limits (--memory, --pids-limit) provide independent protection against resource exhaustion; and this is a universal scanner false positive on every glibc-based image with no upstream fix planned since glibc maintainers consider resource limits on regex evaluation to be the caller's responsibility, not the library's.
CVE-2020-15719	mmorrisj/pgvector	2-Mar-26	Not Applicable	this 2020 finding concerns OpenLDAP's client library (libldap) where certificate validation during a TLS renegotiation/reconnection does not properly enforce the configured TLS_REQCERT setting when using the LDAP_OPT_X_TLS_CONNECT_CB callback, potentially allowing a man-in-the-middle attacker to present an invalid certificate that is accepted during the renegotiated session. This project does not use LDAP in any capacity — no ldap.conf or LDAP URIs are configured, no application code establishes LDAP connections, PostgreSQL authentication uses pg_hba.conf with password-based methods rather than LDAP-passthrough, the registry.Dockerfile explicitly removes postgresql-client and its transitive libldap2 dependency chain, and the vulnerability specifically requires an application that initiates LDAP TLS connections with the CONNECT_CB callback — a usage pattern entirely absent from this project; even where trace libldap libraries may remain in the base Debian image as transitive dependencies, no code loads or calls into them, so the vulnerable TLS renegotiation codepath is unreachable.
CVE-2022-0563	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2022 finding concerns util-linux's chfn and chsh set-uid utilities improperly handling the READLINE variable, allowing a local attacker to inject a crafted shared library path that gets loaded with elevated privileges when an unprivileged user runs these commands, potentially leading to privilege escalation. The Docker containers in this project run as non-root users (USER postgres, USER appuser with /sbin/nologin) with no interactive login shell, no user account modification is performed at runtime, chfn and chsh are never invoked by any entrypoint, script, or application code; Docker's default seccomp profile blocks the TIOCSTI ioctl and constrains set-uid behavior; the containers have no local interactive users who would invoke these utilities; set-uid binaries within containers do not carry effective privileges due to Docker's default no-new-privileges security option and capability restrictions; and even if the binaries exist in the base Debian image, the combination of non-root runtime users, no login shell, and no invocation of the vulnerable utilities means the exploitation path — a local user running chfn/chsh with a crafted READLINE variable — is entirely unreachable.
CVE-2022-27943	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2022 finding concerns a stack exhaustion vulnerability in GCC's libiberty library within the demangle_const function in cp-demangle.c, triggered by crafted C++ mangled symbols that cause excessive recursion during demangling, leading to denial of service. GCC and its associated build tools (build-essential) are installed only at build time in both Dockerfiles to compile pgvector and Python C-extension dependencies, and are explicitly purged before the final image layers — pgvector.Dockerfile removes build-essential and git at line 48, and registry.Dockerfile purges build-essential at line 73 with residual config cleanup; no C++ compilation, linking, or symbol demangling occurs at runtime; the application is written in Python and TypeScript; no untrusted C++ mangled symbols are processed by any service; and even if trace libiberty remnants exist in the base image, the demangle_const function is only invoked by developer tooling (c++filt, nm, objdump, gdb) none of which are present in or called by the final runtime containers.
CVE-2022-3219	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2022 finding concerns GnuPG's dirmngr component being vulnerable to a denial of service via excessive resource consumption when processing a crafted certificate chain or CRL (Certificate Revocation List) during keyserver or LDAP interactions. GnuPG is not required at runtime by this project — the pgvector.Dockerfile explicitly purges gnupg, gpg, gpg-wks-client, and gpg-wks-server at line 49; the registry.Dockerfile never installs GnuPG at all; any transient GnuPG presence in base Debian images is solely for APT package signature verification during build-time apt-get install operations, after which package lists are deleted and no further package operations occur; dirmngr (the specifically vulnerable daemon) is never started as a service in any container; no keyserver lookups, CRL checks, or certificate chain validations are performed through GnuPG; and the non-root runtime users have no GnuPG keyrings, configuration directories, or reason to invoke any gpg-family binary, making the vulnerable codepath entirely unreachable.
CVE-2023-31437	mmorrisj/pgvector	2-Mar-26	Not Applicable	"these three disputed 2023 findings concern weaknesses in systemd 253's journald sealed-log integrity mechanism (field truncation bypassing sealing, unprivileged access to sensitive logged data, and modification of past journal events while evading integrity checks, respectively); they are inapplicable because systemd does not run inside any project container — application containers use supervisord as PID 1 and the database container runs postgres directly — meaning no systemd-journald daemon is started, no sealed journal files are created, rotated, or read, and no journal sealing keys exist; libsystemd0 is present only as a transitive shared-library dependency of the Debian base filesystem (required by util-linux and similar packages) and provides C library functions rather than running daemons, so the vulnerable journald code paths are never entered; all logging goes to stdout/stderr captured by the Docker logging driver with no systemd journal integration, no sd_journal_* API calls, and no /var/log/journal mount in any container; containers run as non-root (appuser) with no privileged access; and the systemd maintainers themselves dispute all three findings, considering the described behaviors expected rather than security vulnerabilities.
CVE-2023-31438	mmorrisj/pgvector	2-Mar-26	Not Applicable	"these three disputed 2023 findings concern weaknesses in systemd 253's journald sealed-log integrity mechanism (field truncation bypassing sealing, unprivileged access to sensitive logged data, and modification of past journal events while evading integrity checks, respectively); they are inapplicable because systemd does not run inside any project container — application containers use supervisord as PID 1 and the database container runs postgres directly — meaning no systemd-journald daemon is started, no sealed journal files are created, rotated, or read, and no journal sealing keys exist; libsystemd0 is present only as a transitive shared-library dependency of the Debian base filesystem (required by util-linux and similar packages) and provides C library functions rather than running daemons, so the vulnerable journald code paths are never entered; all logging goes to stdout/stderr captured by the Docker logging driver with no systemd journal integration, no sd_journal_* API calls, and no /var/log/journal mount in any container; containers run as non-root (appuser) with no privileged access; and the systemd maintainers themselves dispute all three findings, considering the described behaviors expected rather than security vulnerabilities.
CVE-2023-31439	mmorrisj/pgvector	2-Mar-26	Not Applicable	"these three disputed 2023 findings concern weaknesses in systemd 253's journald sealed-log integrity mechanism (field truncation bypassing sealing, unprivileged access to sensitive logged data, and modification of past journal events while evading integrity checks, respectively); they are inapplicable because systemd does not run inside any project container — application containers use supervisord as PID 1 and the database container runs postgres directly — meaning no systemd-journald daemon is started, no sealed journal files are created, rotated, or read, and no journal sealing keys exist; libsystemd0 is present only as a transitive shared-library dependency of the Debian base filesystem (required by util-linux and similar packages) and provides C library functions rather than running daemons, so the vulnerable journald code paths are never entered; all logging goes to stdout/stderr captured by the Docker logging driver with no systemd journal integration, no sd_journal_* API calls, and no /var/log/journal mount in any container; containers run as non-root (appuser) with no privileged access; and the systemd maintainers themselves dispute all three findings, considering the described behaviors expected rather than security vulnerabilities.
CVE-2023-31486	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2023 finding concerns Perl's HTTP::Tiny module (before version 0.083) defaulting to unverified TLS connections, enabling man-in-the-middle attacks; it is inapplicable because the SoftPower_Analytics project contains zero Perl application code — the entire stack is Python (FastAPI, SQLAlchemy, Streamlit), TypeScript (React/Vite), and PostgreSQL — and no .pl or .pm scripts exist or execute at runtime in any container; the application containers (api.Dockerfile, dashboard.Dockerfile, production.Dockerfile, registry.Dockerfile, preprocessing.Dockerfile) all derive from python:3.13-slim-bookworm which does not include Perl; Perl is present only in the pgvector.Dockerfile's postgres:16-bookworm base image as a build-time dependency of PostgreSQL's packaging toolchain (PL/Perl support, build scripts), but no Perl process runs at runtime — the sole running process is the postgres server, which uses its own libpq networking stack and never invokes HTTP::Tiny; all outbound HTTPS calls in the project are made through Python's httpx (v0.25.2), boto3, and openai libraries, each of which performs proper TLS certificate verification by default; and consequently the vulnerable HTTP::Tiny code path is never entered and no attack surface is exposed.
CVE-2024-2236	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-2236 is a timing-based side-channel flaw in libgcrypt's RSA implementation that enables Bleichenbacher-style attacks against PKCS#1 v1.5 RSA decryption by observing timing variations across a large number of trial ciphertexts. As with CVE-2018-6829, libgcrypt20 is present in the image as a transitive dependency of the base image's package verification chain (apt → gnupg → libgcrypt20). PostgreSQL uses OpenSSL, not libgcrypt, for all TLS cryptographic operations including RSA key exchange. No application code performs RSA decryption via libgcrypt, no GnuPG encryption operations occur at runtime, and no network service exposes libgcrypt's RSA implementation to external callers. The attack requires sending a large volume of crafted RSA ciphertexts to a service performing libgcrypt RSA decryption — a precondition that cannot be met when no process invokes libgcrypt's RSA code paths. The library is an unremovable transitive dependency of the base image, and its RSA decryption code path is never invoked at runtime.
CVE-2024-26458	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-26458 is a memory leak in MIT Kerberos 5's pmap_rmt.c where the pmap_rmtcall() function fails to free allocated memory when an xdruint32 call fails, allowing repeated exploitation to exhaust memory. The upstream krb5 developers note that pmap_rmtcall() is "unused by the rest of the krb5 code base and likely unused by anyone else." As with the prior Kerberos library CVEs (CVE-2018-5709), libgssapi-krb5-2, libk5crypto3, libkrb5-3, and libkrb5support0 are transitive dependencies of postgresql-client via libpq → libgssapi-krb5-2 for optional GSSAPI/Kerberos authentication support. No Kerberos authentication, keytab, krb5.conf, GSSAPI configuration, or ONC RPC portmapper operations exist in the container — PostgreSQL connections use password authentication over the Docker bridge network. The vulnerable pmap_rmtcall() function is an ONC RPC portmapper utility unrelated to Kerberos authentication flows and is never called by any code path in the image. The libraries are unremovable transitive dependencies of the postgresql-client package, and the vulnerable pmap_rmt.c code path is never invoked at runtime.
CVE-2024-26461	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-26461 is a memory leak in MIT Kerberos 5's k5sealv3.c within the GSSAPI library, where a bounds check in an encoding function fails to free allocated memory. The upstream krb5 developers note that the leak "happens on a bounds check which likely cannot be triggered with any choice of memory-valid API inputs" — the bounds check was introduced to satisfy a static analysis tool rather than guard against a real-world condition. As with CVE-2024-26458 and the prior Kerberos library CVEs, libgssapi-krb5-2, libk5crypto3, libkrb5-3, and libkrb5support0 are transitive dependencies of postgresql-client via libpq → libgssapi-krb5-2 for optional GSSAPI/Kerberos authentication support. No Kerberos authentication, keytab, krb5.conf, or GSSAPI configuration exists in the container — PostgreSQL connections use password authentication over the Docker bridge network. The GSSAPI wrap functions in k5sealv3.c are never called because no GSSAPI security context is ever established. The libraries are unremovable transitive dependencies of the postgresql-client package, and the vulnerable GSSAPI encoding code path is never invoked at runtime.
CVE-2024-56433	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-56433 is a default-configuration issue in shadow-utils where the subordinate UID/GID ranges assigned in /etc/login.defs (SUB_UID_MIN 100000, SUB_UID_COUNT 65536) can collide with network user UIDs, allowing a local user to leverage newuidmap and user namespaces to assume another user's UID and access their resources (e.g., NFS home directories). Exploitation requires creating user namespaces via newuidmap with subordinate ID mappings and accessing shared network resources (NFS, LDAP-backed users) from the mapped UID. The container runs as USER postgres with no user creation, no useradd invocations, no /etc/subuid or /etc/subgid entries, no NFS mounts, and no network user directory. User namespaces are restricted inside Docker containers by default (--userns-remap is not enabled), and newuidmap requires SUID privileges that are not available to the postgres user. The login and passwd packages are unremovable base-image dependencies, and the vulnerable subordinate UID delegation mechanism is never configured or invoked at runtime.
CVE-2024-7883	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2024-7883 is a vulnerability specific to Arm Cortex-M Security Extensions (CMSE) TrustZone-M embedded firmware, where LLVM-compiled code leaks Secure stack contents to Non-secure state via floating-point registers during Secure→Non-secure function calls. This vulnerability exclusively affects embedded ARM microcontroller firmware built with LLVM for TrustZone-M targets — it has no relevance to x86_64 server containers. The libllvm19 package is present in the postgres:16-trixie base image as a transitive dependency (PostgreSQL's JIT compilation support uses LLVM for query optimization). The container runs on x86_64 architecture with no ARM Cortex-M hardware, no TrustZone-M security boundaries, and no CMSE firmware compilation. Debian explicitly marks this as <ignored> with the note "doesn't affect the default build flags in Debian." The library is an unremovable transitive dependency of PostgreSQL's JIT infrastructure, and the vulnerable CMSE code-generation path is entirely inapplicable to this architecture and runtime environment.
CVE-2025-11731	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-11731 - Not applicable — CVE-2025-11731, like CVE-2025-10911 and CVE-2015-9019, affects the libxslt XSLT processing library. As with those prior libxslt CVEs, libxslt1.1 is present in the image because the official PostgreSQL 16 Debian package is compiled with --with-libxslt to support the deprecated xml2 contrib module's xslt_process() function. No XSLT transformations, XSL stylesheets, or xml2 extension usage exist anywhere in the application — the xml2 extension is not loaded (CREATE EXTENSION xml2 does not appear in any initialization script), and no XSLT processing is ever triggered. The library is an unremovable transitive dependency of the postgresql-16 package, and its vulnerable code paths are never invoked at runtime.
CVE-2025-27587	mmorrisj/pgvector	2-Mar-26	Not Applicable	this disputed 2025 finding describes a Minerva timing side-channel attack against OpenSSL 3.0.0 through 3.3.2's P-384 elliptic curve implementation, where an attacker on the same physical machine can measure EVP_DigestSign execution times to statistically infer ECDSA nonce bit sizes and ultimately extract private keys; it is inapplicable because the vulnerability exclusively affects the PowerPC architecture and this project builds and runs entirely on x86_64 (as confirmed by uname -m and the python:3.13-slim-bookworm / postgres:16-bookworm base images, which are amd64); no PowerPC cross-compilation, emulation, or multi-arch builds exist in any Dockerfile or CI configuration; the OpenSSL project itself disputes the finding, as its security policy explicitly excludes same-physical-system side channels from its threat model and considers the timing signal too small to be feasibly exploitable without co-located attacker processes; and even if the architecture were PowerPC, exploitation would require the attacker to run a process on the same physical host to perform high-resolution timing measurements of cryptographic operations, a scenario incompatible with the project's containerized deployment model.
CVE-2025-5278	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-5278 - Not applicable — CVE-2025-5278 is a heap buffer under-read in GNU coreutils' sort utility where the begfield() function performs unsafe pointer arithmetic when processing the traditional key specification syntax (+POS1) with UINTMAX_MAX as the character position, causing a pointer to reference one byte before the allocated buffer. Exploitation requires a user to run sort with a specially crafted command-line key argument — the vulnerability is debated as a regular bug rather than a security issue since it requires direct control over sort's command-line arguments. No sort commands execute at runtime in the container — the only running process is the PostgreSQL server, which has its own internal sort implementation for query processing and does not invoke GNU sort. The coreutils package is an unremovable base-image dependency, and the vulnerable sort begfield() code path is never executed at runtime.
CVE-2025-6141	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-6141 is a stack-based buffer overflow in GNU ncurses' postprocess_termcap() function in tinfo/parse_entry.c, triggered when processing a maliciously crafted termcap entry. Exploitation requires local access and the ability to supply a crafted termcap/terminfo database to a program linked against ncurses. The libncursesw6 and libtinfo6 libraries are present in the image as transitive dependencies of base-image utilities (e.g., bash, less, util-linux) that provide terminal handling. No interactive terminal sessions, TUI applications, or termcap/terminfo database processing occurs at runtime in the container — the only running process is the PostgreSQL server, which communicates via the wire protocol on port 5432 and does not use ncurses for terminal rendering. No user-supplied termcap entries can reach the vulnerable postprocess_termcap() function. Debian classifies this as <no-dsa> (minor issue). The libraries are unremovable transitive dependencies of the base image, and the vulnerable termcap-parsing code path is never invoked at runtime.
CVE-2025-6297	mmorrisj/pgvector	2-Mar-26	Not Applicable	 this 2025 finding concerns dpkg-deb failing to properly sanitize directory permissions when extracting a .deb package's control member into a temporary directory, allowing adversarial .deb files to leave behind unremovable temporary files that can exhaust disk quota or fill the filesystem (denial of service); it is inapplicable because no Dockerfile, build script, or CI/CD pipeline in this project invokes dpkg-deb for package extraction — all package installation uses standard apt-get install, which handles verification and extraction through APT's own safe code paths rather than direct dpkg-deb -x calls; the only dpkg invocations in the project are dpkg --purge --force-all commands in pgvector.Dockerfile, production.Dockerfile, and registry.Dockerfile that remove residual configuration files from already-purged packages, a cleanup operation on an entirely different code path from the vulnerable extraction logic; no untrusted or user-supplied .deb files are downloaded, processed, or extracted at any stage; all containers that perform apt-get install do so only during Docker image build as root (standard and expected), while runtime processes execute as non-root users (appuser or postgres); and the build-time apt-get operations fetch exclusively from official Debian Bookworm repositories with cryptographic signature verification, meaning no adversarial .deb content reaches the vulnerable dpkg-deb extraction path.
CVE-2025-8732	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2025-8732 is an uncontrolled recursion flaw in libxml2's xmlParseSGMLCatalog() function within the xmlcatalog component, where processing a maliciously crafted SGML catalog file causes unbounded recursion and a crash. The libxml2 maintainer states the security impact is "negligible since the vulnerability requires the use of untrusted SGML catalogs, which is not a recommended practice." As with CVE-2026-0990, libxml2 is present because postgresql-16 is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) parses XML documents — it does not load or process SGML catalog files. No XML_CATALOG_FILES environment variable, /etc/xml/catalog, SGML catalog configuration, or xmlcatalog invocation exists in the container, so the xmlParseSGMLCatalog() code path cannot be reached. The library is an unremovable transitive dependency of the postgresql-16 package, and its SGML catalog-parsing code path is never invoked at runtime.
CVE-2026-0989	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-0989 is an unbounded recursion flaw in libxml2's RelaxNG schema processing where recursive <include> elements cause a stack overflow, leading to a denial-of-service crash. Exploitation requires passing a maliciously crafted RelaxNG schema to libxml2's RelaxNG validation engine. As with CVE-2026-0990 and CVE-2025-8732, libxml2 is present because postgresql-16 is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) performs XML document parsing and XPath queries — it does not perform RelaxNG schema validation. No RelaxNG schemas are loaded, validated against, or processed anywhere in the application. Debian classifies this as <no-dsa> (minor issue). The library is an unremovable transitive dependency of the postgresql-16 package, and its RelaxNG validation code path is never invoked at runtime.
CVE-2026-0992	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-0992 is a resource exhaustion flaw in libxml2's XML catalog processing where repeated <nextCatalog> elements pointing to the same downstream catalog cause redundant traversal of catalog chains, leading to excessive CPU consumption and denial of service. As with CVE-2026-0990 and CVE-2026-0989, exploitation requires supplying a crafted XML catalog file to the libxml2 catalog resolver. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) parses XML documents — it does not load or process XML catalog files. No XML_CATALOG_FILES environment variable, /etc/xml/catalog, or application-level catalog configuration exists in the container, so the XML catalog processing code path cannot be reached. Debian classifies this as <no-dsa> (minor issue), and no fixed package is available for Trixie. The library is an unremovable transitive dependency of the postgresql-16 package, and its catalog-resolution code path is never invoked at runtime.
CVE-2026-1757	mmorrisj/pgvector	2-Mar-26	Not Applicable	CVE-2026-1757 is a memory leak in libxml2's xmllint interactive shell that can be exploited locally to cause a denial-of-service condition through resource exhaustion. The vulnerability is specific to the xmllint command-line tool's interactive shell mode — not to the libxml2 shared library when used programmatically. The xmllint binary is not installed in the container image (only libxml2 the shared library is present as a dependency of postgresql-16), and no interactive xmllint sessions occur at runtime. As with the prior libxml2 CVEs (CVE-2026-0990, CVE-2026-0989, CVE-2026-0992, CVE-2025-8732), libxml2 is present because PostgreSQL is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's programmatic use of libxml2 for XML document parsing does not invoke the xmllint shell code paths. The library is an unremovable transitive dependency of the postgresql-16 package, and the vulnerable xmllint interactive shell code path does not exist in the image.
CVE-2026-22185	mmorrisj/pgvector	2-Mar-26	Not Applicable	"CVE-2026-22185 - Not applicable — CVE-2026-22185 affects the OpenLDAP libldap2 library. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-14159, CVE-2017-17740, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, or GSSAPI/SASL-LDAP binding exists in any container. The library is an unremovable transitive dependency of the postgresql-client package and its code paths are never invoked at runtime.
CVE-2026-3184	mmorrisj/pgvector	2-Mar-26	Not Applicable	this recently disclosed (February 25, 2026) vulnerability affects util-linux on Debian Linux and carries a CVSS v3 score of 9.1 with a network-exploitable vector impacting confidentiality and availability; although full technical details remain in a pending evaluation state across vendors, it is inapplicable to this project because no Dockerfile explicitly installs util-linux (only the minimal subset inherited by the Debian slim base images is present), no application code or shell script invokes any util-linux binary (mount, umount, fdisk, lsblk, nsenter, unshare, uuidgen, column, etc.) at runtime, all production containers enforce cap_drop: ALL (removing CAP_SYS_ADMIN, CAP_NET_ADMIN, and every other capability required to exercise privileged util-linux functionality) and no-new-privileges: true (preventing SUID exploitation), all containers run as non-root users (appuser or postgres) with /sbin/nologin shells, the only network-exposed ports serve FastAPI (8000) and Streamlit (8501) — neither of which routes traffic through any util-linux component — and prior assessments of related util-linux CVEs (e.g., CVE-2022-0563, CVE-2025-14104) in the project's existing CVE ANALYSIS.txt confirm that the containerized, non-root, capability-dropped deployment model renders util-linux attack surfaces unreachable; the CVE should be re-evaluated once the full description is published, but no remediation action is currently required.
TEMP-0290435-0B57B5	mmorrisj/pgvector	2-Mar-26	Not Applicable	TEMP-0290435-0B57B5 relates to tar's rmt (remote tape server) binary, which has insufficient input validation. This finding has already been mitigated in the Dockerfile — the rmt binary is explicitly removed at line 55 of docker/pgvector.Dockerfile: The rmt binary no longer exists in the built image. If this finding still appears in scan results, the scanner is likely matching against the tar package metadata rather than checking for the actual binary's presence. The tar package itself is an unremovable base-image dependency, but the vulnerable rmt component has been phys
TEMP-0517018-A83CE6	mmorrisj/pgvector	2-Mar-26	Not Applicable	TEMP-0517018-A83CE6 is an unassigned Debian security tracker finding affecting the sysvinit-utils package, which provides SysV init utilities such as pidof, killall5, and last. This issue has no formal CVE assignment and is classified as low severity. The sysvinit-utils package is present as a base-image dependency providing basic process management utilities — however, Docker containers do not use SysV init as PID 1. The container's entrypoint starts the PostgreSQL server process directly, and no SysV init scripts, runlevel management, or init-based service control occurs at runtime. No interactive sessions invoke these utilities. The package is an unremovable transitive dependency of the postgres:16-trixie base image, its utilities are never executed at runtime, and the finding lacks both a CVE assignment and a fix from Debian.
TEMP-0628843-DBAD28	mmorrisj/pgvector	2-Mar-26	Not Applicable	TEMP-0628843-DBAD28 is an unassigned Debian security tracker finding affecting the shadow-utils login.defs and passwd packages. As with CVE-2007-5686 and CVE-2024-56433 (prior shadow-utils findings), these packages provide user account management utilities (passwd, useradd, login) and configuration (login.defs). No user account management, password changes, user creation, or interactive login sessions occur at runtime in the container — the only running process is the PostgreSQL server. The container runs as USER postgres (a pre-configured non-root user created by the base image), and no passwd, useradd, login, or other shadow-utils utilities are invoked. The packages are unremovable base-image dependencies of the postgres:16-trixie image, the finding lacks a formal CVE assignment, and the affected user-management code paths are never executed at runtime.
TEMP-0841856-B18BAF	mmorrisj/pgvector	2-Mar-26	Not Applicable	TEMP-0841856-B18BAF is an unassigned Debian security tracker finding related to CVE-2016-7543, describing a privilege escalation to another non-root user via crafted SHELLOPTS/BASHOPTS environment variables in bash. The container runs as USER postgres with no interactive shell sessions — the only running process is the PostgreSQL server communicating via the wire protocol on port 5432. There is no second non-root user in the container to escalate to, no SUID bash binaries are present, and no mechanism exists for an external actor to inject SHELLOPTS/BASHOPTS values into the container environment. bash is an unremovable core base-image dependency, the finding lacks a formal CVE assignment, and the vulnerable shell-variable escalation path has no viable target in a single-user, non-interactive container.



mmorrisj/softpower-analytics:1.5.3

CVE-2026-0861 - Not applicable — CVE requires an attacker to control both the size and alignment parameters to memalign/posix_memalign/aligned_alloc with extreme values (alignment in the range [2^62+1, 2^63]). No application code calls these low-level C memory alignment functions with user-supplied parameters; all memory allocation is managed by the Python interpreter and PostgreSQL, both of which use hardcoded, small alignment values internally. The containers run as non-root with no shell access, and no external input path reaches these allocation functions. Ubuntu classifies this as Medium priority, noting it is "not easily exploitable" since alignment arguments are typically constrained and non-attacker-controlled.
CVE-2025-13034 - Mitigated — curl is used only for plaintext localhost health checks (curl -f http://localhost:8000/api/health); CVE requires QUIC protocol with ngtcp2/GnuTLS backend and --pinnedpubkey certificate pinning, none of which are present in the image.
CVE-2025-13151 - Not applicable — libtasn1-6 is a transitive OS dependency (ASN.1/DER parsing for TLS libraries); no application code performs direct ASN.1 parsing, and all network I/O uses Python-level HTTP clients (httpx, requests, boto3) over standard TLS with OpenSSL, not GnuTLS. The stack buffer overflow in asn1_expand_octet_string requires processing a maliciously crafted ASN.1 payload, which cannot be triggered by the application's outbound-only HTTP calls to trusted endpoints (localhost health checks, AWS S3, OpenAI API).
CVE-2025-14104 - Not applicable — CVE requires local shell access with a valid user account to exploit a heap buffer overread in setpwnam() (SUID login utilities processing 256-byte usernames). Production containers run as non-root (appuser/postgres) with no shell (/sbin/nologin), no SSH, and no interactive login capability; the vulnerable setpwnam.c codepath is only used by tools not invoked by the application. These packages are base OS dependencies of the container filesystem and are never called by application code.
CVE-2025-15281 - Not applicable — CVE requires a program to call the glibc wordexp() function with the specific flag combination WRDE_REUSE | WRDE_APPEND, which triggers uninitialized memory and a potential process abort. No application code (Python, Node.js, or PostgreSQL) invokes wordexp(); this is a niche POSIX shell-expansion C function unused by the runtime stack. The containers have no interactive shell access and run as non-root with /sbin/nologin, eliminating any indirect invocation path.
CVE-2025-7709 - Not applicable — libsqlite3-0 is a transitive OS dependency required by the util-linux package chain on Debian trixie and cannot be safely removed. No application code in the production image uses SQLite; the database is PostgreSQL. The only sqlite3 imports in the codebase exist in legacy pipeline/ingestion scripts which are explicitly excluded from the production container (registry.Dockerfile copies only server/, shared/, services/chat/, and services/dashboard/). The library is never loaded or called at runtime.
CVE-2026-0915 - Not applicable — CVE requires calling the glibc functions getnetbyaddr() or getnetbyaddr_r() with DNS configured as the nsswitch backend for network lookups of a zero-valued network, leaking stack contents to a DNS resolver. No application code (Python, Node.js, or PostgreSQL) invokes these niche network-database lookup functions; all network operations use standard socket/HTTP calls. Containers have no nsswitch DNS-for-networks configuration, and the application only makes outbound HTTP connections to known endpoints (localhost, AWS S3, OpenAI API).
CVE-2026-27171 - Not applicable — CVE affects zlib's crc32_combine64/crc32_combine_gen64 functions where a loop can run without termination, causing CPU exhaustion. This requires direct misuse of the zlib C API with crafted inputs; no application code calls these niche CRC-combining functions. Zlib is present as a base OS compression library used transitively by Python and PostgreSQL for standard gzip/deflate operations, which do not invoke the affected code path. Ubuntu classifies this as Low priority, noting it stems from API misuse with no evidence of real-world exploitation.
CVE-2005-2541 - Not applicable — CVE-2005-2541 is a 21-year-old informational finding (2005) about GNU tar not warning users when extracting files with setuid/setgid bits set. This is documented behavior, not a code defect — tar preserves file permissions by design. The tar binary is a base OS utility used only during Docker image build (apt-get install, pip install); it is never invoked at runtime by application code. Containers run as non-root (appuser/postgres) so even if tar were executed, setuid bits would have no privilege escalation effect. Every major scanner flags this as a perpetual low/informational finding with no upstream fix planned.
CVE-2007-5686 - Not applicable — CVE-2007-5686 is an 19-year-old informational finding (2007) about the initscripts package storing user information in /etc/login.defs readable by local users. This is a design-era documentation issue, not a code defect — modern shadow-utils has long since addressed credential storage. The login and passwd packages are base OS dependencies of the container filesystem; no interactive login is possible (containers run as non-root with /sbin/nologin shell, no SSH, no PAM authentication). Ubuntu officially marks this as "does not apply." Every major scanner perpetually flags this with no upstream fix planned.
CVE-2010-4756 - Not applicable — CVE-2010-4756 is a 16-year-old informational finding (2010) about glibc's glob() function not limiting directory traversal depth, theoretically allowing resource consumption via deeply nested {...} patterns. This is documented behavior; glibc upstream has declined to fix it, classifying it as the caller's responsibility to constrain input. No application code invokes glob() with user-supplied patterns; all file operations use Python-level APIs against known paths within the container. Containers run as non-root with no shell access and no user-facing file path input. Every major scanner perpetually flags this as a low/informational finding with no upstream fix planned.
CVE-2011-3374 - Not applicable — CVE-2011-3374 is a 15-year-old informational finding (2011) about APT not properly verifying 'Release.gpg' signatures, theoretically allowing a MITM to substitute packages during apt-get install. The apt/libapt-pkg packages are only used during Docker image build to install OS dependencies; apt is never invoked at runtime. The final production images clear all apt lists (rm -rf /var/lib/apt/lists/*) and run as non-root (appuser/postgres) with no write access to apt configuration. No package installation occurs inside running containers. Debian upstream has declined to fix this, and every major scanner perpetually flags it as a low/informational finding.
CVE-2011-3389 - Not applicable — CVE-2011-3389 (BEAST attack) is a 15-year-old finding about TLS 1.0 CBC cipher vulnerability allowing plaintext recovery via blockwise chosen-boundary attacks. Modern GnuTLS (libgnutls30t64) defaults to TLS 1.2/1.3 which are not affected; TLS 1.0 is disabled by default in current Debian trixie packages. The library is a transitive OS dependency and is not directly invoked by application code — all Python HTTP clients use OpenSSL for TLS, not GnuTLS. No external-facing TLS termination occurs inside the container (a reverse proxy handles TLS in production). This CVE is universally flagged by scanners as a legacy informational finding with no practical exploitability on modern TLS stacks.
CVE-2011-4116 - Not applicable — CVE-2011-4116 is a 15-year-old finding about Perl's File::Temp module creating temporary files insecurely in /tmp. Perl is not installed in the production images (python:3.13-slim does not include Perl); if perl-base is present, it is a minimal transitive dependency of the Debian package manager used only during build. No application code uses Perl — the runtime stack is entirely Python, Node.js, and PostgreSQL. Containers run as non-root with no shell access, and /tmp is not used by application processes. This is a perpetual low/informational scanner finding with no upstream fix planned.
CVE-2013-4392 - Not applicable — CVE-2013-4392 is a 13-year-old finding about systemd's journald writing to /run/log/journal with world-readable permissions, allowing local users to read log data. Systemd does not run inside Docker containers — the container uses the host kernel's init system, and no journald process exists within the container. The libsystemd0 and libudev1 packages are transitive shared-library dependencies of the Debian base filesystem (required by util-linux and other core packages); no systemd daemon or journal is active at runtime. Containers run as non-root with no shell access. This is a perpetual low/informational scanner finding inapplicable to containerized workloads.
CVE-2015-3276 - Not applicable — CVE-2015-3276 is an 11-year-old finding about OpenLDAP's libldap incorrectly handling certificate hostname verification when multiple CN values are present in a certificate's subject field. The libldap2 library is a transitive dependency of postgresql-client (libpq uses it for LDAP authentication support); no application code performs LDAP operations or uses LDAP authentication. The PostgreSQL connections are made via psycopg2 over TCP to a local Docker network database using password authentication, not LDAP. No LDAP server is configured, contacted, or referenced anywhere in the application. This is a perpetual low/informational scanner finding with no practical attack surface in this architecture.
CVE-2017-14159 - Not applicable — CVE-2017-14159 is an 9-year-old finding about OpenLDAP's slapd daemon failing to properly handle closefrom() during privilege revocation, potentially leaking file descriptors. This affects the LDAP server daemon (slapd), which is not installed or running in any container — only the client library libldap2 is present as a transitive dependency of postgresql-client. No LDAP server or client operations are performed by the application; PostgreSQL connections use password authentication over the Docker bridge network. The vulnerable slapd binary does not exist in the image.
CVE-2017-17740 - Not applicable — CVE-2017-17740 is a 9-year-old finding about OpenLDAP's slapd server crashing when built with contrib/slapd-modules/nops overlay enabled, due to a do_search() function issue causing a denial of service. This affects the LDAP server daemon (slapd) compiled with an optional contrib module — the slapd binary is not installed in any container. Only the client library libldap2 is present as a transitive dependency of postgresql-client, and the vulnerable server-side overlay code does not exist in the client library. No LDAP operations are performed by the application.
CVE-2017-18018 - Not applicable — CVE-2017-18018 is a 9-year-old finding about GNU coreutils chown-core.c having a race condition (TOCTOU) when using the -R -L flags on a path with a symlink, potentially changing ownership of the wrong file. This requires local shell access to craft a symlink race during a chown -R -L invocation. No application code runs chown at runtime; the only chown usage is in the Dockerfile build stage (chown -R appuser:appuser /app) which executes in a controlled, single-threaded build context with no attacker interaction. Containers run as non-root with no shell access, and coreutils is a base OS dependency that cannot be removed.
CVE-2018-20796 - Not applicable — CVE-2018-20796 is a 8-year-old finding about glibc's check_dst_limits_calc_pos_1 function in posix/regexec.c causing uncontrolled resource consumption (stack exhaustion) when processing crafted regular expressions. Exploitation requires passing attacker-crafted regex patterns to glibc's POSIX regcomp()/regexec() functions. The application uses Python's re module which is implemented independently of glibc's POSIX regex engine. No application code passes user-supplied input to glibc-level regex functions. glibc is a core OS dependency that cannot be removed, and upstream has declined to fix this, classifying it as the caller's responsibility to constrain input.
CVE-2018-5709 - Not applicable — CVE-2018-5709 is an 8-year-old finding about MIT Kerberos 5 (krb5) where lib/kadm5/kadm_rpc_xdr.c has an integer overflow when unmarshal data, potentially causing incorrect authorization decisions in the Kerberos admin daemon (kadmind). The Kerberos libraries are transitive dependencies of postgresql-client (libpq supports Kerberos/GSSAPI authentication). No Kerberos authentication is configured or used — PostgreSQL connections use password authentication over the Docker bridge network. No KDC, kadmind, or keytab exists in the container. The vulnerable code path is in the Kerberos admin server, not the client libraries. This is a perpetual low/informational scanner finding with no upstream fix planned.
CVE-2019-1010022 - Not applicable — CVE-2019-1010022 through CVE-2019-1010025 are a set of four disputed 7-year-old findings about glibc's lack of certain exploit mitigations: insufficient ASLR entropy (1010022), lack of stack guard page enforcement (1010023), predictable /proc/self/maps information leak (1010024), and heap memory not being zeroed on free (1010025). These are classified as DISPUTED by MITRE because they describe intentional design decisions and theoretical hardening gaps, not exploitable code defects. Exploitation requires existing arbitrary code execution or local shell access, neither of which is available — containers run as non-root (appuser/postgres) with /sbin/nologin, no SSH, and no interactive access. glibc is an unremovable core OS dependency. Every major scanner perpetually flags these as informational findings with no upstream fix planned.
CVE-2019-1010023 - Not applicable — CVE-2019-1010022 through CVE-2019-1010025 are a set of four disputed 7-year-old findings about glibc's lack of certain exploit mitigations: insufficient ASLR entropy (1010022), lack of stack guard page enforcement (1010023), predictable /proc/self/maps information leak (1010024), and heap memory not being zeroed on free (1010025). These are classified as DISPUTED by MITRE because they describe intentional design decisions and theoretical hardening gaps, not exploitable code defects. Exploitation requires existing arbitrary code execution or local shell access, neither of which is available — containers run as non-root (appuser/postgres) with /sbin/nologin, no SSH, and no interactive access. glibc is an unremovable core OS dependency. Every major scanner perpetually flags these as informational findings with no upstream fix planned.
CVE-2019-1010024 - Not applicable — CVE-2019-1010022 through CVE-2019-1010025 are a set of four disputed 7-year-old findings about glibc's lack of certain exploit mitigations: insufficient ASLR entropy (1010022), lack of stack guard page enforcement (1010023), predictable /proc/self/maps information leak (1010024), and heap memory not being zeroed on free (1010025). These are classified as DISPUTED by MITRE because they describe intentional design decisions and theoretical hardening gaps, not exploitable code defects. Exploitation requires existing arbitrary code execution or local shell access, neither of which is available — containers run as non-root (appuser/postgres) with /sbin/nologin, no SSH, and no interactive access. glibc is an unremovable core OS dependency. Every major scanner perpetually flags these as informational findings with no upstream fix planned.
CVE-2019-1010025 - Not applicable — CVE-2019-1010022 through CVE-2019-1010025 are a set of four disputed 7-year-old findings about glibc's lack of certain exploit mitigations: insufficient ASLR entropy (1010022), lack of stack guard page enforcement (1010023), predictable /proc/self/maps information leak (1010024), and heap memory not being zeroed on free (1010025). These are classified as DISPUTED by MITRE because they describe intentional design decisions and theoretical hardening gaps, not exploitable code defects. Exploitation requires existing arbitrary code execution or local shell access, neither of which is available — containers run as non-root (appuser/postgres) with /sbin/nologin, no SSH, and no interactive access. glibc is an unremovable core OS dependency. Every major scanner perpetually flags these as informational findings with no upstream fix planned.
CVE-2019-9192 - Not applicable — CVE-2019-9192 is a 7-year-old disputed finding about glibc's check_dst_limits_calc_pos_1 function in posix/regexec.c causing uncontrolled recursion and stack exhaustion when processing crafted regular expressions (closely related to CVE-2018-20796). MITRE classifies this as DISPUTED; glibc upstream has declined to fix it, stating the caller is responsible for constraining regex input. The application uses Python's re module, which has its own independent regex engine and does not invoke glibc's POSIX regex functions. No user-supplied input reaches glibc-level regex processing. glibc is an unremovable core OS dependency, and every major scanner perpetually flags this as an informational finding.
CVE-2020-15719 - Not applicable — CVE-2020-15719 is a 6-year-old finding about OpenLDAP's libldap client library not enforcing TLS certificate verification when a LDAP_OPT_X_TLS_DEMAND or LDAP_OPT_X_TLS_HARD option is set and a referral chase occurs to a different host. Exploitation requires an active LDAP connection using TLS with referral chasing enabled. No LDAP connections are made by the application — libldap2 is a transitive dependency of postgresql-client (libpq supports optional LDAP authentication). PostgreSQL connections use password authentication over the Docker bridge network, not LDAP. No LDAP configuration, server URI, or referral chasing exists anywhere in the application or container configuration.
CVE-2021-45346 - Not applicable — CVE-2021-45346 is a 5-year-old disputed finding about a memory leak in SQLite's UPDATE command via sqlite3_open(). MITRE classifies this as DISPUTED; the SQLite maintainers have stated this is not a security vulnerability. As previously noted for CVE-2025-7709, libsqlite3-0 is a transitive OS dependency required by the util-linux package chain on Debian trixie and cannot be removed. No application code in the production image uses SQLite — the database is PostgreSQL. The only sqlite3 references in the codebase are in legacy pipeline scripts explicitly excluded from production containers. The library is never loaded or called at runtime.
CVE-2022-0563 - Not applicable — CVE-2022-0563 is a 4-year-old finding about chfn and chsh utilities in util-linux being susceptible to a readline library history file injection when operating in SUID mode, potentially allowing local information disclosure. The chfn/chsh binaries are not present in python:3.13-slim (they are only in the full util-linux package, not the -minimal variant pulled by slim images). Containers run as non-root (appuser/postgres) with /sbin/nologin — no interactive shell sessions or user account modifications occur at runtime. These packages are core OS dependencies of the container filesystem and cannot be removed. No user interaction or account management is performed by the application.
CVE-2023-31437 - Not applicable — CVE-2023-31437 through CVE-2023-31439 are a set of three disputed 3-year-old findings about systemd's journald: size truncation of journal fields (31437), unprivileged access to sensitive data in logged messages (31438), and race conditions in journal file rotation (31439). MITRE classifies all three as DISPUTED; the systemd maintainers consider these expected behavior. Systemd and journald do not run inside Docker containers — the libsystemd0 and libudev1 packages are transitive shared-library dependencies of the Debian base filesystem, not active daemons. No journal is created, rotated, or read inside the container. Containers run as non-root with no shell access. These are unremovable core OS dependencies that every major scanner perpetually flags as informational findings.
CVE-2024-26458 - Not applicable — CVE-2024-26458 is a memory leak in MIT Kerberos 5's lib/gssapi/krb5/k5sealv3.c in the gss_krb5_wrap() function, causing resource consumption when processing GSSAPI tokens. Exploitation requires an active Kerberos/GSSAPI authentication session. The Kerberos libraries are transitive dependencies of postgresql-client (libpq supports optional GSSAPI authentication); no Kerberos authentication is configured or used — PostgreSQL connections use password authentication over the Docker bridge network. No KDC, keytab, krb5.conf, or GSSAPI configuration exists in the container. The vulnerable GSSAPI wrap function is never invoked, and the libraries are unremovable transitive dependencies of the postgresql-client package.
CVE-2024-26461 - Not applicable — CVE-2024-26461 is a memory leak in MIT Kerberos 5's lib/gssapi/krb5/k5seal.c in the gss_krb5_seal() function, causing resource consumption when processing GSSAPI tokens (closely related to CVE-2024-26458). As with that sibling CVE, exploitation requires an active Kerberos/GSSAPI authentication session. No Kerberos authentication is configured or used — PostgreSQL connections use password authentication over the Docker bridge network. No KDC, keytab, krb5.conf, or GSSAPI configuration exists in any container. The Kerberos libraries are unremovable transitive dependencies of postgresql-client, and the vulnerable GSSAPI seal function is never invoked.
CVE-2024-56433 - Not applicable — CVE-2024-56433 is a finding about the shadow-utils (passwd) package where useradd creates a mail spool file (/var/mail/<user>) owned by the new user even if the group ID was hijacked by a non-root user, potentially allowing unauthorized mailbox access. Exploitation requires local shell access and the ability to pre-create groups before useradd runs. The useradd command is only invoked during Docker image build (RUN groupadd -r appuser && useradd -r -g appuser ...) in a controlled, single-threaded build context with no attacker interaction. No mail spool is created (system users with -r flag skip mail creation). Containers run as non-root with /sbin/nologin, no SSH, and no interactive user account management at runtime. These are unremovable base OS packages.
CVE-2025-10966 - Mitigated — curl and libcurl have been removed from all production Docker images in this session (commit 759acbb). The apt-get install curl directive was removed from registry.Dockerfile, production.Dockerfile, dashboard.Dockerfile, and api.Dockerfile, and HEALTHCHECK commands were replaced with Python urllib.request. CVE-2025-10966 no longer applies as the affected packages are not present in the final images.
CVE-2025-14017 - Mitigated — curl and libcurl have been removed from all production Docker images in this session (commit 759acbb). The apt-get install curl directive was removed from registry.Dockerfile, production.Dockerfile, dashboard.Dockerfile, and api.Dockerfile, and HEALTHCHECK commands were replaced with Python urllib.request. CVE-2025-10966 no longer applies as the affected packages are not present in the final images.
CVE-2025-5278 - CVE-2024-2236Not applicable — CVE-2025-5278 is a heap buffer under-read in GNU coreutils sort utility's begfield() function when using the traditional key format (+POS -POS) with crafted input. Exploitation requires local shell access and user interaction to invoke sort with a malicious key specification. No application code invokes the sort command at runtime; coreutils is a base OS dependency used only during Docker image build. Containers run as non-root (appuser/postgres) with /sbin/nologin, no shell access, and no user-facing command execution. Coreutils is an unremovable core OS package.
CVE-2025-6141 - Not applicable — CVE-2025-6141 is a finding about the ncurses library, which provides terminal handling capabilities (screen drawing, cursor movement, keyboard input). The libncursesw6 and libtinfo6 packages are transitive dependencies of the Debian base filesystem (required by bash, less, readline, and other core utilities). No application code uses ncurses terminal manipulation — the runtime stack is Python (FastAPI/Streamlit) serving HTTP, not interactive terminal applications. Containers run as non-root with /sbin/nologin, no TTY allocation, and no interactive terminal sessions. These are unremovable core OS dependencies that cannot be exploited without local interactive shell access to the container.
CVE-2026-22185 - Not applicable — CVE-2026-22185 affects the OpenLDAP libldap2 library. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-14159, CVE-2017-17740, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, or GSSAPI/SASL-LDAP binding exists in any container. The library is an unremovable transitive dependency of the postgresql-client package and its code paths are never invoked at runtime.
CVE-2026-26013 - Not exploitable in current architecture — CVE-2026-26013 is a blind SSRF (CVSS 3.7 Low) in ChatOpenAI.get_num_tokens_from_messages() when processing vision-model image URLs. The application does not use ChatOpenAI, get_num_tokens_from_messages(), or any langchain-openai vision features — no code imports or references these classes. The fix requires langchain-core >=1.2.11 which is incompatible with the current langchain 0.3.x pin (0.3.x hard-requires langchain-core <0.4.0); upgrading requires a full langchain 1.x migration including langgraph dependencies. Given the vulnerable code path is never invoked and the SSRF is blind with a 5-second timeout, this is accepted risk on the current version pin pending the planned langchain 1.x migration.
TEMP-0517018-A83CE6 - All containers run as non-root users (USER appuser / USER postgres), which prevents the local privilege escalation path. The vulnerability relates to the Debian expert installer's "no-root" option, a scenario that does not exist inside Docker containers. No code changes required; accepted as residual risk with existing controls.
TEMP-0628843-DBAD28 - All containers run as non-root users (USER appuser / USER postgres) with no sudo installed and no interactive login sessions. The underlying CVE-2005-4890 terminal handling issue has no exploitable path in a non-interactive Docker container. No code changes required; accepted as residual risk with existing controls
TEMP-0841856-B18BAF -  Containers run a single non-root user with no interactive shell sessions and no SUID binaries. The escalation path (to a different non-root user) has no viable target in a single-user container. No code changes required; accepted as residual risk with existing controls.

mmorisj/pgvector 0.8.1 pg16

CVE-2026-0861 - Not applicable — CVE-2026-0861 affects the glibc memalign family of functions (memalign, posix_memalign, aligned_alloc), where an excessively large alignment value triggers an integer overflow that can corrupt the heap. The vulnerability requires a local process to call these functions with attacker-controlled alignment parameters. PostgreSQL does not use glibc's aligned allocation APIs — it manages memory through its own palloc/MemoryContext subsystem, and no application code in the container invokes memalign with user-supplied values. The container runs as USER postgres with no interactive shell access; the only entry point is the PostgreSQL wire protocol on port 5432. Debian classifies this as <no-dsa> (minor issue), and no fixed glibc package is available for Trixie. The affected packages (libc-bin, libc-l10n, libc6, locales) are unremovable core dependencies of every binary in the image and their vulnerable code paths are never reached at runtime.
CVE-2025-10911 - Not applicable — CVE-2025-10911 is a use-after-free in libxslt's XSLT processing engine triggered while parsing XSL key-related data across result value trees. libxslt1.1 is present in the image because the official PostgreSQL 16 Debian package is compiled with --with-libxslt to support the deprecated xml2 contrib module's xslt_process() function. No XSLT transformations, XSL stylesheets, or xml2 extension usage exist anywhere in the application — the database handles structured relational and pgvector embedding queries exclusively. The xml2 extension is not loaded (CREATE EXTENSION xml2 does not appear in any initialization script), and the vulnerable xsltApplyTemplates / key-handling code paths are never invoked at runtime. The library is an unremovable transitive dependency of the postgresql-16 package and cannot be purged without breaking the database server.
CVE-2025-14104 - Not applicable — CVE-2025-14104 is a heap buffer overread in util-linux's setpwnam() function triggered when processing 256-byte usernames through SUID login utilities (passwd, chfn, chsh) that write to the system password database. The container runs as USER postgres (a non-root user), so SUID binaries cannot be executed to modify /etc/passwd or the shadow store. No interactive shell sessions, user management operations, or password database writes occur within the container — it exclusively serves PostgreSQL wire-protocol connections on port 5432. Debian classifies this as <no-dsa> (minor issue) across all current releases. The affected packages (bsdutils, libblkid1, liblastlog2-2, libmount1, libsmartcols1, libuuid1, login, mount, util-linux) are core base-image utilities required by the postgres:16-trixie base image and cannot be removed without breaking fundamental system functionality. The vulnerable setpwnam() code path is never reachable at runtime.
CVE-2025-15281 - Not applicable — CVE-2025-15281 is a flaw in glibc's wordexp() function where calling it with the WRDE_REUSE and WRDE_APPEND flags together may return uninitialized memory in the we_wordv member, causing a subsequent wordfree() call to abort the process. wordexp() is a POSIX shell-expansion API used to perform shell-like word expansion (tilde, variable, glob) on arbitrary strings — it is not used by PostgreSQL, pgvector, or any code path in the application. PostgreSQL's SQL parser, query planner, and extension loading do not invoke POSIX word expansion, and no application script or initialization logic calls wordexp(). The affected packages (libc-bin, libc6) are unremovable core dependencies of every binary in the image, Debian has no fixed glibc version available for Trixie, and the vulnerable code path is never reached at runtime.
CVE-2025-7709 - Not applicable — CVE-2025-7709 is an integer overflow in SQLite's FTS5 (Full-Text Search 5) extension where tombstone-pointer array size calculation truncates to 32 bits, enabling an out-of-bounds write. As noted in the pgvector Dockerfile comments (line 39–40), libsqlite3-0 is present because it is required by the util-linux dependency chain on Trixie and cannot be safely purged without breaking base package dependencies. PostgreSQL uses its own storage engine and full-text search implementation (tsvector/tsquery) — it does not use SQLite or its FTS5 extension for any purpose. No application code opens an SQLite database, loads the FTS5 extension, or invokes any SQLite API. Debian classifies this as <no-dsa> (minor issue) across Trixie and Bookworm, and no fixed package is available for Trixie. The library is an unremovable transitive dependency of util-linux, its FTS5 code path is never loaded or executed, and the preconditions for exploitation (crafting a malicious FTS5 database) cannot be met in a PostgreSQL-only container.
CVE-2026-0915 - Not applicable — CVE-2026-0915 is an information disclosure flaw in glibc's getnetbyaddr() and getnetbyaddr_r() functions, present since 1996, where a zero-valued network query causes the DNS resolver to construct a query from uninitialized stack bytes, potentially leaking adjacent stack contents to a DNS server. PostgreSQL does not call getnetbyaddr() or getnetbyaddr_r() — it resolves hosts via getaddrinfo() for forward DNS lookups and uses pg_hba.conf network matching against already-parsed CIDR notation, not reverse network-to-name resolution. No application code performs network-value-to-name lookups, and the specific precondition (passing a zero-valued network argument) has no plausible trigger within a PostgreSQL container that accepts connections on a fixed Docker bridge address. Debian classifies this as <no-dsa> (minor issue), no fixed glibc is available for Trixie, and the affected packages (libc-bin, libc6) are unremovable core dependencies whose vulnerable getnetbyaddr code path is never invoked at runtime.
CVE-2026-0990 - Not applicable — CVE-2026-0990 is an uncontrolled recursion flaw in libxml2's xmlCatalogXMLResolveURI() function, triggered when an XML catalog contains a self-referencing delegate URI entry, causing infinite recursion and a segfault. Exploitation requires providing a specially crafted XML catalog file — not merely an XML document. PostgreSQL's XML support (xml data type, xpath(), xmlparse()) uses libxml2 for document parsing but does not load or process XML catalog files. No XML_CATALOG_FILES environment variable, /etc/xml/catalog, or application-level catalog configuration exists in the container, so the xmlCatalogXMLResolveURI code path cannot be reached. The library is present because the postgresql-16 package is compiled with --with-libxml for SQL/XML standard support, and it is an unremovable transitive dependency. Debian classifies this as <no-dsa> (minor issue), no fixed package is available for Trixie, and the vulnerable catalog-resolution code path is never invoked at runtime.
CVE-2026-27171 - Not applicable — CVE-2026-27171 is a CPU exhaustion flaw in zlib's crc32_combine64() and crc32_combine_gen64() functions, where the internal x2nmodp routine can enter an infinite loop due to a missing termination condition on right-shift operations. These functions are used to combine CRC-32 checksums from independently compressed data segments — a specialized operation for parallel compression workflows. PostgreSQL does not call crc32_combine64() or crc32_combine_gen64(); it uses its own CRC implementation (pg_crc32c) for WAL checksums and data integrity, and its zlib usage is limited to pg_dump --compress / COPY ... WITH (FORMAT csv, HEADER, COMPRESS zlib) which invokes deflate()/inflate(), not CRC combination. No application code performs parallel CRC merging. The CVSS score is 2.9 (LOW), attack complexity is high, and Debian classifies it as <no-dsa> (minor issue) across all releases. zlib1g is an unremovable core dependency of the base image, and the vulnerable crc32_combine64 code path is never invoked at runtime.
CVE-2005-2541 - Not applicable — CVE-2005-2541 documents GNU tar's longstanding behavior of not warning users when extracting archives containing setuid or setgid files. Both Debian and Red Hat classify this as intended behavior, not a bug — tar is an archiving tool and the --preserve-permissions (-p) flag is explicitly documented. No tar extraction operations occur at runtime in the container; the image is built once and runs only the PostgreSQL server process. Even if tar were invoked, the container runs as USER postgres (non-root), so extracted setuid/setgid bits would have no privilege-escalation effect — only root can create effective setuid binaries. This CVE has been open since 2005 with no fix planned by any distribution because the behavior is by design. The tar package is an unremovable base-image utility, and its documented permission-preservation behavior poses no risk in a non-root, non-interactive container that never extracts archives at runtime.C
CVE-2007-5686 - Not applicable — CVE-2007-5686 is a permissions flaw in rPath Linux's initscripts package that set insecure permissions on /var/log/btmp (the failed-login-attempts log), allowing local users to read authentication attempt data and causing sshd to skip logging failed logins. This CVE is specific to rPath Linux's initscripts — not to the login or passwd (shadow-utils) packages themselves. It appears in container scans because scanners match the CVE against the login/passwd package namespace broadly. On Debian, btmp permissions are managed correctly by the login package with mode 0660 (root:utmp), and LOG_UNKFAIL_ENAB is set to no in login.defs per Debian bug #290803 so unknown usernames are never recorded. The container runs as USER postgres with no SSH daemon, no interactive login sessions, and no local users reading log files. The login and passwd packages are unremovable base-image dependencies of the postgres:16-trixie image, the underlying rPath-specific misconfiguration does not exist on Debian, and no authentication logging code path is exercised at runtime.
CVE-2010-4756 - Not applicable — CVE-2010-4756 is a resource-exhaustion flaw in glibc's glob() function where crafted glob expressions that match no pathnames cause excessive CPU and memory consumption. The documented attack vector is remote authenticated users sending malicious STAT commands to FTP daemons that pass user-supplied patterns to glob(). No FTP server, shell glob expansion service, or any process that passes external input to glob() runs in the container — the only network-facing service is PostgreSQL on port 5432, which uses its own SQL parser and does not invoke glibc's glob() with user-supplied patterns. This CVE dates to 2010 with no fix issued by glibc upstream, as it is considered a misuse-of-API concern rather than a library defect. Debian has never issued an advisory for it. The affected packages (libc-bin, libc6) are unremovable core dependencies of every binary in the image, and the vulnerable glob() code path is never reachable through PostgreSQL's wire protocol.
CVE-2011-3374 - Not applicable — CVE-2011-3374 describes apt-key's failure to correctly validate GPG keys against the master keyring, creating a theoretical man-in-the-middle vector during package repository key management. The apt-key utility and apt-get update/install are only invoked at image build time to compile the pgvector extension, after which all build tools (including ca-certificates) are purged and /var/lib/apt/lists/* is removed. No apt operations occur at runtime — the container runs only the PostgreSQL server process. The affected net-update feature was specific to Ubuntu and has been permanently disabled since 2012 (CVE-2012-0954). Debian classifies this as "unimportant" and has never issued an advisory. The apt and libapt-pkg7.0 packages are unremovable base-image dependencies required by the Debian package management layer, the apt-key code path is never executed at runtime, and no package repository interactions occur in the running container.
CVE-2011-4116 - Not applicable — CVE-2011-4116 is a symlink-following flaw in Perl's File::Temp module where the _is_safe() function does not properly validate symlinks before accessing temporary files, allowing a local attacker to trick a Perl script into following a malicious symlink in /tmp. No Perl scripts execute at runtime in the container — the only running process is the PostgreSQL server. Perl is present in the postgres:16-trixie base image because PostgreSQL's build and packaging toolchain uses it (e.g., PL/Perl support, build-time scripting), but no application code invokes Perl or uses File::Temp to create temporary files. Exploitation requires a local attacker with write access to the container's /tmp directory to race a Perl process that is creating temporary files — a precondition that cannot be met when no Perl process runs. The affected packages (libperl, perl, perl-base, perl-modules) are unremovable transitive dependencies of the PostgreSQL base image, and the vulnerable File::Temp::_is_safe() code path is never executed at runtime.
CVE-2013-4392 - Not applicable — CVE-2013-4392 is a TOCTOU race condition in systemd-tmpfiles where a local attacker can exploit a symlink race during file permission and SELinux context updates to modify permissions on arbitrary files. systemd-tmpfiles does not run inside Docker containers — containers do not use systemd as PID 1, and no tmpfiles cleanup or permission-adjustment operations are performed. The PostgreSQL container's entrypoint starts the postgres process directly, not through systemd. The affected packages (libsystemd0, libudev1) are present solely as shared library dependencies of other base-image packages (e.g., util-linux links against libsystemd0 for journal logging) — the systemd daemon and systemd-tmpfiles binary are not installed in the image. Additionally, the vulnerability requires both local user access and an active SELinux enforcement context, neither of which exists in a Docker container running as USER postgres. The libraries are unremovable transitive dependencies, and the vulnerable systemd-tmpfiles code path does not exist in the image.
CVE-2015-3276 - Not applicable — CVE-2015-3276 affects the OpenLDAP libldap2 library, where the nss_parse_ciphers() function incorrectly parses OpenSSL-style cipher strings with a + prefix, potentially selecting unintended ciphers during TLS negotiation with an LDAP server. As with the prior libldap2 CVEs (CVE-2017-14159, CVE-2017-17740, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, TLS cipher configuration, or SASL-LDAP binding exists in any container. The vulnerability's precondition — initiating a TLS-secured LDAP connection with a cipher string containing + prefixes — cannot be met. The library is an unremovable transitive dependency of the postgresql-client package and its cipher-parsing code path is never invoked at runtime.
CVE-2015-9019 - Not applicable — CVE-2015-9019 is a flaw in libxslt where the xsltKeyComputeStored() function does not properly initialize a newly allocated xmlXPathObject, allowing pattern matching to use uninitialized memory as a node set, producing unpredictable and potentially exploitable results during XSLT key resolution. As with CVE-2025-10911, libxslt1.1 is present in the image because the official PostgreSQL 16 Debian package is compiled with --with-libxslt to support the deprecated xml2 contrib module's xslt_process() function. No XSLT transformations, XSL stylesheets, or xml2 extension usage exist anywhere in the application — the xml2 extension is not loaded (CREATE EXTENSION xml2 does not appear in any initialization script), and no XSLT key resolution or pattern matching is ever triggered. The library is an unremovable transitive dependency of the postgresql-16 package, and its xsltKeyComputeStored() code path is never invoked at runtime.
CVE-2017-14159 - Not applicable — CVE-2017-14159 is a flaw in OpenLDAP's slapd where the ldap_pvt_thread_pool_getkey() function allows out-of-bounds memory access when the thread pool is not yet initialized, potentially crashing the LDAP server process. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-17740, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP server (slapd) runs in the container, and no LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, or SASL-LDAP binding exists in any container. The vulnerability specifically affects the LDAP server daemon's thread pool initialization, a component that is not installed in the image. The library is an unremovable transitive dependency of the postgresql-client package and its thread-pool code path is never invoked at runtime.
CVE-2017-17740 - Not applicable — CVE-2017-17740 is a flaw in OpenLDAP's slapd where the contrib/slapd-modules/nops/nops.c module, when both the nops overlay and memberof overlay are enabled, attempts to free a buffer.bv_val pointer that is not dynamically allocated, causing a double-free or crash. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-14159, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP server (slapd) runs in the container, and neither the nops nor memberof overlay modules are installed or configured — the vulnerability is specific to an optional contrib module in the LDAP server daemon, which is not present in the image. No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. The library is an unremovable transitive dependency of the postgresql-client package and the affected slapd contrib module code path does not exist in the image.
CVE-2017-18018 - Not applicable — CVE-2017-18018 is a TOCTOU race condition in GNU coreutils' chown and chgrp commands when invoked with the recursive symlink-following flags (-R -L), allowing a local attacker to replace a file with a symlink mid-traversal and redirect ownership changes to arbitrary files. No chown -R -L or chgrp -R -L operations occur at runtime in the container — the only running process is the PostgreSQL server. The container runs as USER postgres (non-root), so even if chown were invoked, it would lack the privileges to change ownership of files it does not own. The vulnerability is considered unfixable upstream since the -R -L flag combination is POSIX-specified behavior, and the "fix" is a documentation warning. coreutils is an unremovable base-image package, and the vulnerable chown -R -L code path is never executed at runtime.
CVE-2018-20796 - Not applicable — CVE-2018-20796 is an uncontrolled recursion flaw in glibc's POSIX regex engine (check_dst_limits_calc_pos_1 in posix/regexec.c) where a crafted regular expression pattern causes unbounded stack recursion, crashing the process. Exploitation requires a local user to pass a maliciously crafted regex to a program that uses glibc's regexec() function. PostgreSQL uses its own regex engine (derived from Henry Spencer's library, not glibc's posix/regexec.c) for SQL ~, ~*, SIMILAR TO, and substring() pattern matching — it does not call glibc's regexec() with user-supplied patterns. The glibc maintainers themselves do not treat this as a vulnerability per the glibc Security Exceptions policy, as it requires a deliberately crafted pattern. No fixed glibc version exists for any Debian release. The affected packages (libc-bin, libc6) are unremovable core dependencies of every binary in the image, and the vulnerable regexec() code path is never reached with user-supplied input at runtime.
CVE-2018-5709 - Not applicable — CVE-2018-5709 is an integer overflow in MIT Kerberos 5's kadmin/dbutil/dump.c where the 16-bit dbentry->n_key_data field is assigned from a 32-bit variable during Kerberos database dump processing. Exploitation requires access to the kadmin database utility operating on a crafted Kerberos dump file — a tool and workflow exclusive to Kerberos KDC administration. No Kerberos KDC, kadmin utility, or Kerberos database exists in the container. The Kerberos libraries (libgssapi-krb5-2, libk5crypto3, libkrb5-3, libkrb5support0) are transitive dependencies of postgresql-client via libpq → libgssapi-krb5-2 for optional GSSAPI/Kerberos authentication support. No Kerberos authentication, keytab, krb5.conf, or GSSAPI configuration exists in any container — PostgreSQL connections use password authentication over the Docker bridge network. Debian classifies this as a "non-issue" since the affected code path only runs on trusted input. The libraries are unremovable transitive dependencies of the postgresql-client package, and the vulnerable kadmin dump-processing code path is not installed in the image.
CVE-2018-6829 - Not applicable — CVE-2018-6829 is a cryptographic weakness in libgcrypt's ElGamal implementation where plaintexts are improperly encoded, breaking the Decisional Diffie-Hellman assumption and allowing information leakage under a ciphertext-only attack when ElGamal is used to encrypt messages directly. Debian's security team explicitly states this is "not a vulnerability in libgcrypt, but in an application using it in an insecure manner." No application code in the container uses libgcrypt's ElGamal cipher for direct message encryption — libgcrypt20 is present as a transitive dependency of the base image's package verification chain (apt → gnupg → libgcrypt20). PostgreSQL uses OpenSSL for its TLS connections, not libgcrypt, and no GnuPG encryption, ElGamal key generation, or direct libgcrypt API calls occur at runtime. The library is an unremovable transitive dependency of the base image, and its ElGamal encryption code path is never invoked.
CVE-2019-1010022 - All four are classified by the glibc project as "not treated as a security issue" and are explicitly ignored by projects such as Buildroot in their CVE tracking. None represent exploitable attack vectors in a PostgreSQL container — no users run ldd, no interactive shell sessions exist, and exploitation of all four requires pre-existing local code execution. The affected packages (libc-bin, libc6) are unremovable core dependencies, no fixes have been or will be issued by any distribution, and the disputed status means these are scanner noise rather than actionable findings.
CVE-2019-1010023 - All four are classified by the glibc project as "not treated as a security issue" and are explicitly ignored by projects such as Buildroot in their CVE tracking. None represent exploitable attack vectors in a PostgreSQL container — no users run ldd, no interactive shell sessions exist, and exploitation of all four requires pre-existing local code execution. The affected packages (libc-bin, libc6) are unremovable core dependencies, no fixes have been or will be issued by any distribution, and the disputed status means these are scanner noise rather than actionable findings.
CVE-2019-1010024 - All four are classified by the glibc project as "not treated as a security issue" and are explicitly ignored by projects such as Buildroot in their CVE tracking. None represent exploitable attack vectors in a PostgreSQL container — no users run ldd, no interactive shell sessions exist, and exploitation of all four requires pre-existing local code execution. The affected packages (libc-bin, libc6) are unremovable core dependencies, no fixes have been or will be issued by any distribution, and the disputed status means these are scanner noise rather than actionable findings.
CVE-2019-1010025 - All four are classified by the glibc project as "not treated as a security issue" and are explicitly ignored by projects such as Buildroot in their CVE tracking. None represent exploitable attack vectors in a PostgreSQL container — no users run ldd, no interactive shell sessions exist, and exploitation of all four requires pre-existing local code execution. The affected packages (libc-bin, libc6) are unremovable core dependencies, no fixes have been or will be issued by any distribution, and the disputed status means these are scanner noise rather than actionable findings.
CVE-2019-9192 - Not applicable — CVE-2019-9192 is an uncontrolled recursion flaw in glibc's POSIX regex engine (check_dst_limits_calc_pos_1 in posix/regexec.c) where a crafted regular expression causes unbounded stack recursion, crashing the process. This is closely related to CVE-2018-20796 and affects the same function. The glibc maintainers dispute this CVE, stating it is not a vulnerability because the behavior only occurs with a deliberately crafted pattern — the glibc Security Exceptions policy does not treat regex stack exhaustion from crafted patterns as a security issue. As with CVE-2018-20796, PostgreSQL uses its own regex engine (derived from Henry Spencer's library, not glibc's posix/regexec.c) for SQL ~, ~*, SIMILAR TO, and substring() pattern matching and does not pass user-supplied input to glibc's regexec(). No fixed glibc version exists for any Debian release, and Debian does not treat this as a vulnerability. The affected packages (libc-bin, libc6) are unremovable core dependencies of every binary in the image, and the vulnerable regexec() code path is never reached with user-supplied input at runtime.
CVE-2020-15719 - Not applicable — CVE-2020-15719 is a flaw in OpenLDAP's libldap where certificate verification during a TLS-secured LDAP connection is not enforced when the connection referral hop limit is exceeded, potentially allowing a man-in-the-middle attacker to present an invalid certificate during a referral chase. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-14159, CVE-2017-17740), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, TLS-secured LDAP session, or referral-chasing configuration exists in any container. The vulnerability's precondition — an active TLS-secured LDAP connection following referrals past the hop limit — cannot be met. The library is an unremovable transitive dependency of the postgresql-client package and its referral-chasing TLS verification code path is never invoked at runtime.
CVE-2021-45346 - Not applicable — CVE-2021-45346 is a memory leak in SQLite where a maliciously crafted database file with tampered record metadata causes sqlite3VdbeSerialGet to read beyond record boundaries, leaking adjacent memory contents. The SQLite developers dispute this as a vulnerability, noting that an attacker who can modify the raw database file already has full read access to its contents, making the information disclosure moot. As with CVE-2025-7709, libsqlite3-0 is present in the image solely because it is required by the util-linux dependency chain on Trixie. PostgreSQL does not use SQLite — it has its own storage engine, and no application code opens, queries, or processes SQLite database files. The precondition for exploitation (supplying a maliciously crafted .sqlite file to a process that opens and queries it) cannot be met in a container that exclusively runs the PostgreSQL server. The library is an unremovable transitive dependency of util-linux, and no SQLite code path is ever executed at runtime.
CVE-2022-0563 - Not applicable — CVE-2022-0563 is an information disclosure flaw in util-linux's chfn and chsh utilities when compiled with Readline support, where an unprivileged user can set the INPUTRC environment variable to point at a root-owned file, causing Readline to print its contents in error messages. Exploitation requires an interactive shell session where a local user invokes the SUID chfn or chsh binaries with a crafted INPUTRC value. The container runs as USER postgres with no interactive shell sessions, no local users, and no mechanism to invoke chfn or chsh. Additionally, Debian's util-linux packages are not compiled with Readline support for these utilities, eliminating the vulnerable code path entirely. The affected packages (bsdutils, libblkid1, liblastlog2-2, libmount1, libsmartcols1, libuuid1, login, mount, util-linux) are unremovable core base-image dependencies, and the vulnerable Readline-based input code path in chfn/chsh is neither compiled in nor executed at runtime.
CVE-2023-31437 - Not applicable — CVE-2023-31437, CVE-2023-31438, and CVE-2023-31439 are three related flaws in systemd 253's sealed journal log file integrity mechanism, allowing an attacker with write access to sealed journal files to conceal log entries (31437), truncate and resume sealing without integrity errors (31438), or modify past log event contents while passing integrity checks (31439). The upstream systemd developers dispute all three as security vulnerabilities. Docker containers do not use systemd as PID 1, do not run systemd-journald, and do not create or maintain sealed journal log files — PostgreSQL logs to stderr/container stdout, not to the systemd journal. The affected packages (libudev1, libsystemd0) are present solely as shared library dependencies of other base-image packages (e.g., util-linux links against libsystemd0 for optional journal integration) — the systemd daemon, systemd-journald, and the journal sealing infrastructure are not installed in the image. The libraries are unremovable transitive dependencies, and the vulnerable journal-sealing code paths do not exist in the container.
CVE-2023-31438 - Not applicable — CVE-2023-31437, CVE-2023-31438, and CVE-2023-31439 are three related flaws in systemd 253's sealed journal log file integrity mechanism, allowing an attacker with write access to sealed journal files to conceal log entries (31437), truncate and resume sealing without integrity errors (31438), or modify past log event contents while passing integrity checks (31439). The upstream systemd developers dispute all three as security vulnerabilities. Docker containers do not use systemd as PID 1, do not run systemd-journald, and do not create or maintain sealed journal log files — PostgreSQL logs to stderr/container stdout, not to the systemd journal. The affected packages (libudev1, libsystemd0) are present solely as shared library dependencies of other base-image packages (e.g., util-linux links against libsystemd0 for optional journal integration) — the systemd daemon, systemd-journald, and the journal sealing infrastructure are not installed in the image. The libraries are unremovable transitive dependencies, and the vulnerable journal-sealing code paths do not exist in the container.
CVE-2023-31439 - Not applicable — CVE-2023-31437, CVE-2023-31438, and CVE-2023-31439 are three related flaws in systemd 253's sealed journal log file integrity mechanism, allowing an attacker with write access to sealed journal files to conceal log entries (31437), truncate and resume sealing without integrity errors (31438), or modify past log event contents while passing integrity checks (31439). The upstream systemd developers dispute all three as security vulnerabilities. Docker containers do not use systemd as PID 1, do not run systemd-journald, and do not create or maintain sealed journal log files — PostgreSQL logs to stderr/container stdout, not to the systemd journal. The affected packages (libudev1, libsystemd0) are present solely as shared library dependencies of other base-image packages (e.g., util-linux links against libsystemd0 for optional journal integration) — the systemd daemon, systemd-journald, and the journal sealing infrastructure are not installed in the image. The libraries are unremovable transitive dependencies, and the vulnerable journal-sealing code paths do not exist in the container.
CVE-2024-2236 - Not applicable — CVE-2024-2236 is a timing-based side-channel flaw in libgcrypt's RSA implementation that enables Bleichenbacher-style attacks against PKCS#1 v1.5 RSA decryption by observing timing variations across a large number of trial ciphertexts. As with CVE-2018-6829, libgcrypt20 is present in the image as a transitive dependency of the base image's package verification chain (apt → gnupg → libgcrypt20). PostgreSQL uses OpenSSL, not libgcrypt, for all TLS cryptographic operations including RSA key exchange. No application code performs RSA decryption via libgcrypt, no GnuPG encryption operations occur at runtime, and no network service exposes libgcrypt's RSA implementation to external callers. The attack requires sending a large volume of crafted RSA ciphertexts to a service performing libgcrypt RSA decryption — a precondition that cannot be met when no process invokes libgcrypt's RSA code paths. The library is an unremovable transitive dependency of the base image, and its RSA decryption code path is never invoked at runtime.
CVE-2024-26458 - Not applicable — CVE-2024-26458 is a memory leak in MIT Kerberos 5's pmap_rmt.c where the pmap_rmtcall() function fails to free allocated memory when an xdruint32 call fails, allowing repeated exploitation to exhaust memory. The upstream krb5 developers note that pmap_rmtcall() is "unused by the rest of the krb5 code base and likely unused by anyone else." As with the prior Kerberos library CVEs (CVE-2018-5709), libgssapi-krb5-2, libk5crypto3, libkrb5-3, and libkrb5support0 are transitive dependencies of postgresql-client via libpq → libgssapi-krb5-2 for optional GSSAPI/Kerberos authentication support. No Kerberos authentication, keytab, krb5.conf, GSSAPI configuration, or ONC RPC portmapper operations exist in the container — PostgreSQL connections use password authentication over the Docker bridge network. The vulnerable pmap_rmtcall() function is an ONC RPC portmapper utility unrelated to Kerberos authentication flows and is never called by any code path in the image. The libraries are unremovable transitive dependencies of the postgresql-client package, and the vulnerable pmap_rmt.c code path is never invoked at runtime.
CVE-2024-26461 - Not applicable — CVE-2024-26461 is a memory leak in MIT Kerberos 5's k5sealv3.c within the GSSAPI library, where a bounds check in an encoding function fails to free allocated memory. The upstream krb5 developers note that the leak "happens on a bounds check which likely cannot be triggered with any choice of memory-valid API inputs" — the bounds check was introduced to satisfy a static analysis tool rather than guard against a real-world condition. As with CVE-2024-26458 and the prior Kerberos library CVEs, libgssapi-krb5-2, libk5crypto3, libkrb5-3, and libkrb5support0 are transitive dependencies of postgresql-client via libpq → libgssapi-krb5-2 for optional GSSAPI/Kerberos authentication support. No Kerberos authentication, keytab, krb5.conf, or GSSAPI configuration exists in the container — PostgreSQL connections use password authentication over the Docker bridge network. The GSSAPI wrap functions in k5sealv3.c are never called because no GSSAPI security context is ever established. The libraries are unremovable transitive dependencies of the postgresql-client package, and the vulnerable GSSAPI encoding code path is never invoked at runtime.
CVE-2024-56433 - Not applicable — CVE-2024-56433 is a default-configuration issue in shadow-utils where the subordinate UID/GID ranges assigned in /etc/login.defs (SUB_UID_MIN 100000, SUB_UID_COUNT 65536) can collide with network user UIDs, allowing a local user to leverage newuidmap and user namespaces to assume another user's UID and access their resources (e.g., NFS home directories). Exploitation requires creating user namespaces via newuidmap with subordinate ID mappings and accessing shared network resources (NFS, LDAP-backed users) from the mapped UID. The container runs as USER postgres with no user creation, no useradd invocations, no /etc/subuid or /etc/subgid entries, no NFS mounts, and no network user directory. User namespaces are restricted inside Docker containers by default (--userns-remap is not enabled), and newuidmap requires SUID privileges that are not available to the postgres user. The login and passwd packages are unremovable base-image dependencies, and the vulnerable subordinate UID delegation mechanism is never configured or invoked at runtime.
CVE-2024-7883 - Not applicable — CVE-2024-7883 is a vulnerability specific to Arm Cortex-M Security Extensions (CMSE) TrustZone-M embedded firmware, where LLVM-compiled code leaks Secure stack contents to Non-secure state via floating-point registers during Secure→Non-secure function calls. This vulnerability exclusively affects embedded ARM microcontroller firmware built with LLVM for TrustZone-M targets — it has no relevance to x86_64 server containers. The libllvm19 package is present in the postgres:16-trixie base image as a transitive dependency (PostgreSQL's JIT compilation support uses LLVM for query optimization). The container runs on x86_64 architecture with no ARM Cortex-M hardware, no TrustZone-M security boundaries, and no CMSE firmware compilation. Debian explicitly marks this as <ignored> with the note "doesn't affect the default build flags in Debian." The library is an unremovable transitive dependency of PostgreSQL's JIT infrastructure, and the vulnerable CMSE code-generation path is entirely inapplicable to this architecture and runtime environment.
CVE-2025-11731 - Not applicable — CVE-2025-11731, like CVE-2025-10911 and CVE-2015-9019, affects the libxslt XSLT processing library. As with those prior libxslt CVEs, libxslt1.1 is present in the image because the official PostgreSQL 16 Debian package is compiled with --with-libxslt to support the deprecated xml2 contrib module's xslt_process() function. No XSLT transformations, XSL stylesheets, or xml2 extension usage exist anywhere in the application — the xml2 extension is not loaded (CREATE EXTENSION xml2 does not appear in any initialization script), and no XSLT processing is ever triggered. The library is an unremovable transitive dependency of the postgresql-16 package, and its vulnerable code paths are never invoked at runtime.
CVE-2025-5278 - Not applicable — CVE-2025-5278 is a heap buffer under-read in GNU coreutils' sort utility where the begfield() function performs unsafe pointer arithmetic when processing the traditional key specification syntax (+POS1) with UINTMAX_MAX as the character position, causing a pointer to reference one byte before the allocated buffer. Exploitation requires a user to run sort with a specially crafted command-line key argument — the vulnerability is debated as a regular bug rather than a security issue since it requires direct control over sort's command-line arguments. No sort commands execute at runtime in the container — the only running process is the PostgreSQL server, which has its own internal sort implementation for query processing and does not invoke GNU sort. The coreutils package is an unremovable base-image dependency, and the vulnerable sort begfield() code path is never executed at runtime.
CVE-2025-6141 - Not applicable — CVE-2025-6141 is a stack-based buffer overflow in GNU ncurses' postprocess_termcap() function in tinfo/parse_entry.c, triggered when processing a maliciously crafted termcap entry. Exploitation requires local access and the ability to supply a crafted termcap/terminfo database to a program linked against ncurses. The libncursesw6 and libtinfo6 libraries are present in the image as transitive dependencies of base-image utilities (e.g., bash, less, util-linux) that provide terminal handling. No interactive terminal sessions, TUI applications, or termcap/terminfo database processing occurs at runtime in the container — the only running process is the PostgreSQL server, which communicates via the wire protocol on port 5432 and does not use ncurses for terminal rendering. No user-supplied termcap entries can reach the vulnerable postprocess_termcap() function. Debian classifies this as <no-dsa> (minor issue). The libraries are unremovable transitive dependencies of the base image, and the vulnerable termcap-parsing code path is never invoked at runtime.
CVE-2025-8732 - Not applicable — CVE-2025-8732 is an uncontrolled recursion flaw in libxml2's xmlParseSGMLCatalog() function within the xmlcatalog component, where processing a maliciously crafted SGML catalog file causes unbounded recursion and a crash. The libxml2 maintainer states the security impact is "negligible since the vulnerability requires the use of untrusted SGML catalogs, which is not a recommended practice." As with CVE-2026-0990, libxml2 is present because postgresql-16 is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) parses XML documents — it does not load or process SGML catalog files. No XML_CATALOG_FILES environment variable, /etc/xml/catalog, SGML catalog configuration, or xmlcatalog invocation exists in the container, so the xmlParseSGMLCatalog() code path cannot be reached. The library is an unremovable transitive dependency of the postgresql-16 package, and its SGML catalog-parsing code path is never invoked at runtime.
CVE-2026-0989 - Not applicable — CVE-2026-0989 is an unbounded recursion flaw in libxml2's RelaxNG schema processing where recursive <include> elements cause a stack overflow, leading to a denial-of-service crash. Exploitation requires passing a maliciously crafted RelaxNG schema to libxml2's RelaxNG validation engine. As with CVE-2026-0990 and CVE-2025-8732, libxml2 is present because postgresql-16 is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) performs XML document parsing and XPath queries — it does not perform RelaxNG schema validation. No RelaxNG schemas are loaded, validated against, or processed anywhere in the application. Debian classifies this as <no-dsa> (minor issue). The library is an unremovable transitive dependency of the postgresql-16 package, and its RelaxNG validation code path is never invoked at runtime.
CVE-2026-0992 - Not applicable — CVE-2026-0992 is a resource exhaustion flaw in libxml2's XML catalog processing where repeated <nextCatalog> elements pointing to the same downstream catalog cause redundant traversal of catalog chains, leading to excessive CPU consumption and denial of service. As with CVE-2026-0990 and CVE-2026-0989, exploitation requires supplying a crafted XML catalog file to the libxml2 catalog resolver. PostgreSQL's XML functionality (xml data type, xpath(), xmlparse()) parses XML documents — it does not load or process XML catalog files. No XML_CATALOG_FILES environment variable, /etc/xml/catalog, or application-level catalog configuration exists in the container, so the XML catalog processing code path cannot be reached. Debian classifies this as <no-dsa> (minor issue), and no fixed package is available for Trixie. The library is an unremovable transitive dependency of the postgresql-16 package, and its catalog-resolution code path is never invoked at runtime. 
CVE-2026-1757 - Not applicable — CVE-2026-1757 is a memory leak in libxml2's xmllint interactive shell that can be exploited locally to cause a denial-of-service condition through resource exhaustion. The vulnerability is specific to the xmllint command-line tool's interactive shell mode — not to the libxml2 shared library when used programmatically. The xmllint binary is not installed in the container image (only libxml2 the shared library is present as a dependency of postgresql-16), and no interactive xmllint sessions occur at runtime. As with the prior libxml2 CVEs (CVE-2026-0990, CVE-2026-0989, CVE-2026-0992, CVE-2025-8732), libxml2 is present because PostgreSQL is compiled with --with-libxml for SQL/XML standard support. PostgreSQL's programmatic use of libxml2 for XML document parsing does not invoke the xmllint shell code paths. The library is an unremovable transitive dependency of the postgresql-16 package, and the vulnerable xmllint interactive shell code path does not exist in the image.
CVE-2026-22185 - Not applicable — CVE-2026-22185 affects the OpenLDAP libldap2 library. As with the prior libldap2 CVEs (CVE-2015-3276, CVE-2017-14159, CVE-2017-17740, CVE-2020-15719), libldap2 is a transitive dependency of postgresql-client (libpq links against it for optional LDAP authentication support). No LDAP connections, configuration, or operations are performed by the application — PostgreSQL connections use password authentication over the Docker bridge network. No LDAP server URI, ldap.conf, or GSSAPI/SASL-LDAP binding exists in any container. The library is an unremovable transitive dependency of the postgresql-client package and its code paths are never invoked at runtime.
TEMP-0290435-0B57B5 - Not applicable — TEMP-0290435-0B57B5 relates to tar's rmt (remote tape server) binary, which has insufficient input validation. This finding has already been mitigated in the Dockerfile — the rmt binary is explicitly removed at line 55 of docker/pgvector.Dockerfile: The rmt binary no longer exists in the built image. If this finding still appears in scan results, the scanner is likely matching against the tar package metadata rather than checking for the actual binary's presence. The tar package itself is an unremovable base-image dependency, but the vulnerable rmt component has been phys
TEMP-0517018-A83CE6 - Not applicable — TEMP-0517018-A83CE6 is an unassigned Debian security tracker finding affecting the sysvinit-utils package, which provides SysV init utilities such as pidof, killall5, and last. This issue has no formal CVE assignment and is classified as low severity. The sysvinit-utils package is present as a base-image dependency providing basic process management utilities — however, Docker containers do not use SysV init as PID 1. The container's entrypoint starts the PostgreSQL server process directly, and no SysV init scripts, runlevel management, or init-based service control occurs at runtime. No interactive sessions invoke these utilities. The package is an unremovable transitive dependency of the postgres:16-trixie base image, its utilities are never executed at runtime, and the finding lacks both a CVE assignment and a fix from Debian.
TEMP-0628843-DBAD28 - Not applicable — TEMP-0628843-DBAD28 is an unassigned Debian security tracker finding affecting the shadow-utils login.defs and passwd packages. As with CVE-2007-5686 and CVE-2024-56433 (prior shadow-utils findings), these packages provide user account management utilities (passwd, useradd, login) and configuration (login.defs). No user account management, password changes, user creation, or interactive login sessions occur at runtime in the container — the only running process is the PostgreSQL server. The container runs as USER postgres (a pre-configured non-root user created by the base image), and no passwd, useradd, login, or other shadow-utils utilities are invoked. The packages are unremovable base-image dependencies of the postgres:16-trixie image, the finding lacks a formal CVE assignment, and the affected user-management code paths are never executed at runtime.
TEMP-0841856-B18BAF - Not applicable — TEMP-0841856-B18BAF is an unassigned Debian security tracker finding related to CVE-2016-7543, describing a privilege escalation to another non-root user via crafted SHELLOPTS/BASHOPTS environment variables in bash. The container runs as USER postgres with no interactive shell sessions — the only running process is the PostgreSQL server communicating via the wire protocol on port 5432. There is no second non-root user in the container to escalate to, no SUID bash binaries are present, and no mechanism exists for an external actor to inject SHELLOPTS/BASHOPTS values into the container environment. bash is an unremovable core base-image dependency, the finding lacks a formal CVE assignment, and the vulnerable shell-variable escalation path has no viable target in a single-user, non-interactive container.


mmorrisj/softpower-analytics 1.8.36 python3.13-slim-trixie / mmorrisj/pgvector 0.8.2-pg17-bookworm	5August2026

CVE-2026-13221	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-13221 (CVSS 9.1 CRITICAL nominal) is an integer overflow in the Perl regex engine's trie optimization (Perl_study_chunk in regcomp_study.c): compiling a regex alternation with more than 65,535 fixed-string branches overflows a 16-bit delta field, silently producing wrong match results (introduced in perl 5.37.10, fixed upstream in 5.43.10). perl-base is present in the Debian-based images only as an Essential base package. The application is entirely Python/TypeScript — no Perl code exists in the repository, no entrypoint, cron, or pipeline invokes the perl interpreter, and nothing compiles attacker-supplied Perl regexes, let alone a 65k-branch alternation. The pgvector image's Bookworm perl 5.36 predates the bug's introduction outright; the trixie app image's perl 5.40 is in the affected range but the interpreter is never executed, so despite the critical score there is no reachable code path.
CVE-2026-18477	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-18477 is a TOCTOU race condition (CWE-367) in GNU tar's --listed-incremental restore path: a local attacker with write access to a directory involved in an incremental restore can cause files to be created or overwritten outside the intended extraction directory (CVSS 4.4 MEDIUM, AV:L/AC:H/UI:R per Red Hat; no upstream fixed release yet). tar is an Essential Debian package in the images but is never executed at runtime — backup and restore flows in this project use pg_dump, parquet exports, and S3 via boto3, never tar incremental backups. Exploitation additionally requires an untrusted local user sharing the restore directory and an operator actively running the restore, neither of which exists in these single-purpose containers.
CVE-2026-18508	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-18508 is a link-following flaw (CWE-59) in GNU tar's --one-top-level option: hardlink targets in a crafted archive are not confined to the top-level directory, so extracting an untrusted archive over a preexisting symlink can read or overwrite files outside the intended boundary (CVSS 4.4 MEDIUM, local, requires user interaction). Default extraction without --one-top-level is not impacted. No container process extracts tar archives at all — no application script, Dockerfile step, or deploy script uses --one-top-level, and Python's tarfile module is likewise unused in the codebase — so the vulnerable option is never exercised.
CVE-2026-50812	softpower-analytics	5-Aug-26	Not Applicable	Not applicable — CVE-2026-50812 is a NULL pointer dereference (CWE-476) in SQLite's Session Extension: sqlite3changeset_apply_v3() crashes when applying a corrupt changeset blob (CVSS 5.5 MEDIUM, local, availability-only). libsqlite3 is present in the app image solely as a dependency of the Python standard library. The application's only datastore is PostgreSQL with pgvector — no module in the repository imports sqlite3, and the vulnerable changeset APIs are C-level Session Extension entry points that Python's stdlib sqlite3 module does not even expose. Since no component applies SQLite changesets, trusted or otherwise, the vulnerable code is unreachable.
CVE-2026-50813	softpower-analytics	5-Aug-26	Not Applicable	Not applicable — CVE-2026-50813 is the companion to CVE-2026-50812: a heap buffer over-read (CWE-126) in the SQLite Session Extension's changeset concat/changegroup merge path when processing a corrupt changeset, allowing local information disclosure (CVSS 6.1 CNA / 5.5 ADP, MEDIUM). The same reasoning applies — SQLite is present only as a Python stdlib dependency, the application exclusively uses PostgreSQL, nothing imports sqlite3, and the sqlite3changeset_concat/changegroup APIs are not exposed through any code path in this project. Unreachable in this deployment.
CVE-2026-53910	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-53910 covers signed integer overflows in diff3's line-mapping calculations (diffutils <= 3.12) that can lead to an undersized allocation and out-of-bounds heap writes when diff3 processes attacker-controlled diff output (CVSS 4.0 score 2.1 LOW, local; upstream maintainers dispute the security impact, stating the worst case is a crash with no privilege gain). diffutils is a required base package in the Debian images, but diff3 is never invoked by any application component, container entrypoint, or build step, and exploitation requires an attacker who already controls a local diff3 invocation's input or --diff-program — a position from which no additional access is gained in these containers.
CVE-2026-56391	softpower-analytics	5-Aug-26	Not Applicable	Not applicable — CVE-2026-56391 is an out-of-bounds read (CWE-125) in coreutils uniq versions 9.5-9.11: find_field() miscalculates multibyte character lengths when the -w/--check-chars option is used, reading past the input buffer (CVSS 4.6 MEDIUM, local, requires active user interaction). Only the trixie-based app image is even in range (coreutils 9.7); the bookworm-based pgvector image ships coreutils 9.1, which predates the affected range, and redis:7-alpine uses busybox rather than GNU coreutils. No script or runtime process in this project invokes uniq at all — exploitation requires an attacker who already has a shell and controls the command line and input, at which point no vulnerability is needed to read process memory they own.
CVE-2026-56392	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-56392 is a heap-based buffer overflow (CWE-122) in coreutils unexpand (<= 9.11) via an integer overflow in allocation sizing when extremely large -t tab-stop values are supplied (CVSS 1.8 LOW, local, attack complexity requiring attacker-controlled command-line arguments). unexpand is never executed anywhere in this stack — not by application code, entrypoints, build steps, or deploy scripts — and the containers expose no mechanism for an external actor to run coreutils commands with chosen arguments. A LOW-severity local finding in an unused utility; scanner noise for this image set.
CVE-2026-57432	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-57432 is an integer overflow (CWE-190/125) in Perl's S_measure_struct: a pack/unpack template whose computed size wraps negative bypasses the length guard and leaks out-of-bounds heap memory (CISA-ADP CVSS 8.4 HIGH but attack vector Local; fixed in perl 5.43.11, no Debian packages patched yet). The stated exploitation prerequisite is an application passing pack/unpack templates derived from untrusted input. As with CVE-2026-13221, perl exists in the Debian images only as the Essential perl-base package and is never executed by any application, scheduler, or entrypoint code path in this project, so no pack/unpack call — let alone one with untrusted templates — ever occurs.
CVE-2026-57433	softpower-analytics + pgvector	5-Aug-26	Not Applicable	Not applicable — CVE-2026-57433 is an integer overflow (CWE-190) in the Storable module bundled with perl-base (Storable < 3.41): a crafted SX_HOOK record with an I32_MAX item count wraps negative and panics the interpreter during Storable::thaw/retrieve deserialization. CISA-ADP scored it 9.8 CRITICAL (AV:N/C:H/I:H/A:H), but the concretely described impact is a deserialization crash, and the score presumes an application that deserializes attacker-supplied Storable blobs over the network — a pattern Storable's own documentation already warns is unsafe. No Perl code runs in any container in this project, nothing calls Storable, and no Perl deserialization of external data exists anywhere in the stack. The critical score is scanner noise for this deployment; the package will be picked up automatically when Debian publishes a patched perl.

Section note (5-Aug-26): All ten findings above are Debian base-layer OS package CVEs (perl, GNU tar, SQLite, diffutils, coreutils) surfaced by image scanning, not vulnerabilities in any Python or npm dependency of the application. Nine of ten are local-attack-vector; the one network-scored entry (CVE-2026-57433) requires a Perl Storable deserialization path that does not exist in this stack. As of this analysis no fixed Debian packages have been published for any of them (Ubuntu tracker: 'needs evaluation' across releases; upstream fixes exist only as commits or unreleased versions), so no image rebuild can remediate them yet. Remediation is the existing routine: rebuild images when Debian trixie/bookworm point releases land. redis:7-alpine is unaffected by all ten (musl/busybox, no GNU coreutils/diffutils/tar, no perl); node:22-bookworm-slim is a build stage only and ships no runtime layers.


mmorrisj/softpower-analytics 2.0.0 python3.13-slim-trixie / mmorrisj/pgvector 0.8.2-pg17-bookworm	14August2026

CVE-2026-15534	softpower-analytics + pgvector	14-Aug-26	Not Applicable	Not applicable — CVE-2026-15534 is an integer overflow (CWE-190) in the Perl regex engine's superlinear-match (WHILEM) cache in regexec.c: a signed 32-bit cache counter overflows when a regex with 15+ nested quantified groups is matched against an attacker-controlled subject string of roughly 286 MB or larger, producing out-of-bounds heap reads/writes (CVSS 5.7 MEDIUM, AV:L/AC:H; affects every released Perl through 5.45.1, upstream fix commits only, no distro packages patched yet). As with the prior Perl findings (CVE-2026-13221, -57432, -57433), perl exists in the Debian-based images solely as the Essential perl-base package: the application is entirely Python/TypeScript, no entrypoint or pipeline invokes the perl interpreter, and nothing evaluates Perl regexes over any input, let alone 286 MB of untrusted data through a 15-quantifier pattern. No reachable code path.
CVE-2026-6368	softpower-analytics + pgvector	14-Aug-26	Not Applicable	Not applicable — CVE-2026-6368 is a use-of-uninitialized-resource bug (CWE-908) in glibc's wordexp() POSIX word-expansion API: calling wordexp with the WRDE_APPEND flag can leave invalid memory in we_wordv, and a subsequent wordfree() aborts the process (CVSS 4.0 score 2.1 LOW, local, high complexity; affects glibc < 2.43 — bookworm 2.36 and trixie 2.41 are both in range; fixed only in glibc 2.43, no distro backports yet). Exploitation requires the application itself to call wordexp() with WRDE_APPEND. Nothing in this stack does: Python's standard library does not use wordexp (os.path.expanduser and glob are pure-Python), PostgreSQL does not call wordexp (already established for the related CVE-2025-15281 finding), and no application C extension exposes the API. The vulnerable function is never invoked, and the worst-case impact is a self-inflicted crash of a process that would have to opt into the API first.
CVE-2026-6791	softpower-analytics + pgvector	14-Aug-26	Not Applicable	Not applicable — CVE-2026-6791 is a stack-clash overflow (CWE-121) in glibc's wordexp() tilde expansion (parse_tilde): an unbounded stack allocation for the username in an input like ~<thousands-of-characters> exhausts thread stack space and crashes the process (CVSS 4.0 score 6.6 MEDIUM, nominally network vector but only insofar as an application feeds remotely supplied strings into wordexp; affects glibc through 2.43, fix on the 2.43 stable branch/2.44+, no Debian packages patched yet). The precondition is the same as CVE-2026-6368: some component must pass untrusted input to wordexp(). No Python, PostgreSQL, Redis, or application code path in this project calls wordexp — shell tilde expansion in bash uses bash's own expander, and Python performs tilde expansion in pure Python — so the network-flavored score does not translate into any exposure for these containers. redis:7-alpine is out of scope entirely (musl libc, not glibc).

Section note (14-Aug-26): All three findings are OS base-layer library CVEs (perl, glibc) with no application-level exposure: the dependency set is unchanged between releases 1.8.36 and 2.0.0 (no requirements or npm lockfile changes), and the new 2.0.0 server code (survey router, guided tour) introduces no native calls. Both glibc issues require the application to call the wordexp() API and the Perl issue requires executing Perl regexes over ~286 MB of untrusted input — none of which occurs anywhere in this stack. As of this analysis no fixed Debian packages exist for any of the three (Ubuntu: 'needs evaluation' across releases); remediation remains routine base-image rebuilds once patched glibc/perl packages land in trixie/bookworm.
