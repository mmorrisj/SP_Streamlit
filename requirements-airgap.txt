# ============================================
# Air-Gapped Runtime Requirements (SLIM)
# Lightweight packages baked into the Docker image.
# Heavy ML packages (torch, sentence-transformers, langchain-huggingface)
# are installed separately from wheels — see requirements-airgap-heavy.txt
# ============================================

# Core
python-dotenv
pandas>=2.2.0
numpy>=2.1.0
sqlalchemy>=2.0.37,<2.1
psycopg2-binary>=2.9.10
psycopg[binary]>=3.1.0
python-docx
pyyaml
requests
matplotlib>=3.9.0

# Database & Migrations
alembic==1.12.1
pgvector==0.2.4

# LangChain 1.x ecosystem
# Must be listed BEFORE requirements-airgap-heavy.txt installs langchain-huggingface,
# so pip resolves langchain-huggingface to 1.x (not 0.1.2 which pins langchain-core <0.4.0).
langchain>=1.0
langchain-core>=1.0

# AI/LLM
openai>=2.14.0
boto3

# API Service (FastAPI)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0
python-multipart
pydantic
pydantic-settings

# Authentication
PyJWT>=2.8.0
bcrypt>=4.0.0

# Dashboard (Streamlit)
streamlit>=1.40.0
plotly>=5.18.0
altair>=5.2.0
pyvis

# Utilities
pyarrow>=18.0.0
filelock>=3.20.3
wheel>=0.46.2
pillow>=12.1.1
